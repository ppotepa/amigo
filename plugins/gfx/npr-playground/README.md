# NPR playground

Opt-in comic-ink gallery with a separate engine parameter panel.

Run `cargo run -p amigo-app -- --hosted --mod npr-playground --scene gallery`
or select `cube`. Models: cube, wedge, 24-sided cylinder, icosphere, Suzanne and
Avocado. Imported assets are pinned locally under the authored mod.

The panel edits typed runtime-control metadata: animation pause/speed/step,
selection, per-object transforms and style overrides, global ink/light, camera,
debug views, seed and scene presets. Drag in the render window to orbit; scroll to
zoom. FPS measures rendered-frame wall time. Width and wobble are in pixels.

Wheel zoom uses a proportional target with exponential easing (about 90% of the
motion within 130 ms). Viewport input is applied once per host frame in PostUpdate,
independent of simulation catch-up and animation pause. Fit, selection and explicit
distance edits cancel a previous zoom destination; bounds remain 0.1–100 units.

YAML owns scenes/layout only; Rhai handles declared actions; mesh import belongs to
the mesh domain; projection, feature extraction and tessellation belong to
`amigo-render-npr`. No plugin-specific logic is added to the app or WGPU backend.

`cargo test -p amigo-npr-playground-plugin` covers controls, invalid presets,
pause/step, six-model import, layout bindings and render snapshot/resize behavior.

## Workshop panel

The object strip has geometry thumbnails, previous/next selection and state badges:
W = rendered, R = rotating, S = custom style. Thumbnails use neutral fixed poses,
not live animation. Their typed triangle artwork is generated by the
`panel_artwork` example and embedded at the end of the authored layout.

Playback and FPS stay pinned around six tabs: appearance, object, scene, camera,
presets and diagnostics. Single-object selection fits the camera; gallery selection
preserves it. Per-object rotation switches survive global pause.
An optional cyan marker identifies the selected object in the rendered gallery.

Appearance edits explicitly target the global style or selected object's override.
Enabling an override copies the current global look. Built-in Comic Ink, Blueprint
and Soft Toon looks are Rust-owned. Appearance-only presets (`npr-look`) are
shown separately from the built-in thumbnail choices. They are
separate from full scene presets (`npr-playground`); names share the scene folder,
and overwriting another kind is rejected. Reset buttons and actions are undoable.

Undo/redo retains 128 edits and coalesces repeated edits of the same property within
500 ms. History stores only changed fields, so unrelated animation is not rewound.
Capture/Before compares styles at the current pose/camera without mutating settings;
editing is disabled while Before is shown. History and comparison clear on scene load.

## Humanized drawing controls

`Pencil Study`, `Loose Study`, `Confident Ink` and `Broad Nib` are typed Rust
profiles, not shader presets. Pencil and brush response combines confidence,
pressure, correction passes, endpoint taper and a deterministic seed-derived
gesture. The correction pass is deliberately faint; it gives hesitant lines a
second attempt without turning every edge into a sketchy halo.

Paper tooth/grain affect the paper and stroke coverage independently. Ink
dryness reduces coverage and increases local grain. Tone density adds clipped,
screen-space hatching inside shaded faces; spacing, angle and cross-hatching
are explicit controls. All of these are recomputed when the viewport or camera
changes, so line widths remain in pixels and identical inputs remain stable.
