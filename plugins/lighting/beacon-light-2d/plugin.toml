id = "amigo.lighting.beacon-light-2d"
family = "lighting"
kind = "semantic-source"
renderable = true
render_participation = "source-renderer"

[capabilities]
provides = []
requires = ["lighting.light.2d@1"]

[slots]
implements = []
requires = []
replaces = []

[targets]
reads = []
writes = ["SceneLighting", "SceneColor"]
contributes = ["SceneHighlight", "SceneEmissive"]

[contributions]
emits = [
  { domain = "camera.optics", type = "CameraOpticsContribution2d", policy = "ExplicitOnly" }
]
consumes = []

[diagnostics]
channels = ["beacon-light-2d.contributions"]

[docs]
pipeline = "docs/pipeline.md"
contributions = "docs/contributions.md"
diagnostics = "docs/diagnostics.md"

[tests]
waterfall = "tests/waterfall_tests.rs"
diagnostics = "tests/diagnostics_tests.rs"
