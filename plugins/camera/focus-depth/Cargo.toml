[package]
name = "amigo-focus-depth-plugin"
version = "0.1.0"
edition = "2021"

[dependencies]
amigo-plugin-api = { path = "../../../crates/core/plugin-api" }
amigo-camera-core-plugin = { path = "../camera-core" }
