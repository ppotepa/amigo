Poniżej jest plan dla agenta na bazie snapshotu `concat-output-r6gdf`. Linie są z aktualnego snapshotu i mogą się przesunąć po pierwszych commitach. Agent ma lokalizować symbole przez `rg`, nie przez ślepe numery linii.

Cel: dojść z obecnego **8.6/10** do realnego **9.0–9.1/10** przez usunięcie pozostałych centralnych katalogów, special-case’ów i app/runtime-bundles monolitu.

# Zasady pracy dla agenta

Nie robić na początku:

```bash
cargo check --workspace
cargo test --workspace
cargo clippy --workspace
cargo fmt --all
```

Używać małych walidacji:

```bash
cargo metadata --no-deps
cargo check -p amigo-scene
cargo check -p amigo-runtime-bundles
cargo check -p amigo-devtools
cargo check -p amigo-render-api
cargo check -p amigo-render-wgpu
cargo check -p amigo-app
```

Do szukania:

```bash
rg "FullRuntimeSceneResetHandler"
rg "ComponentKind::"
rg "default_component_registry"
rg "ConsoleRhaiValueKind"
rg "Renderable2dPayload"
rg "amigo_runtime_bundles::amigo_"
rg "amigo_camera_core_plugin"
rg "amigo_camera_optics_plugin"
rg "amigo_render_wgpu"
```

Nie robić:

```text
1. Nie usuwać pluginów.
2. Nie spłaszczać architektury.
3. Nie przenosić logiki pluginów z powrotem do app.
4. Nie dodawać zależności plugins/* do engine/scene, engine/render-api, engine/devtools.
5. Nie dodawać render-wgpu do devtools.
6. Nie ukrywać problemów przez nowe #[allow].
7. Nie robić pełnego render packet rewrite w jednym commicie.
8. Nie ruszać naraz scene metadata, render packet i app runtime w jednym commicie.
```

Docelowa zasada:

```text
plugin rejestruje własny reset / metadata / commands / completions / extractors
runtime/bundles tylko składa pluginy
app tylko hostuje
```

---

# Priorytet 0 — naprawić build sanity i brakujące tools

## P0.1 — root workspace wskazuje nieistniejące `crates/tools/*`

### Problem

W `Cargo.toml` workspace members:

```toml
58 "crates/tools/scene-preview",
59 "crates/tools/scene-snapshot",
60 "crates/tools/amigo-codemap",
61 "crates/tools/amigo-symbol-explorer",
62 "crates/tools/plugin-check",
63 "crates/tools/plugin-new",
```

W snapshotcie nie ma realnych `crates/tools/*/Cargo.toml`.

### Operacje

#### MODIFY `Cargo.toml`

Jeżeli w realnym repo te katalogi nie istnieją, tymczasowo zakomentować/usunąć z `[workspace].members`:

```toml
# "crates/tools/scene-preview",
# "crates/tools/scene-snapshot",
# "crates/tools/amigo-codemap",
# "crates/tools/amigo-symbol-explorer",
# "crates/tools/plugin-check",
# "crates/tools/plugin-new",
```

oraz usunąć z `[workspace.dependencies]`:

```toml
amigo-symbol-explorer = { path = "crates/tools/amigo-symbol-explorer" }
```

Jeżeli katalogi istnieją w realnym repo, nic nie usuwać. Tylko uruchomić:

```bash
cargo metadata --no-deps
```

### Walidacja

```bash
cargo metadata --no-deps
```

### Dlaczego

Bez tego agent może utknąć na workspace zanim zacznie właściwe refaktory.

---

# Priorytet 1 — usunąć centralny scene reset z `runtime/bundles`

Obecnie jest lepiej niż wcześniej, bo `app` używa `SceneResetHandlerRegistry`, ale reset jest nadal centralny w:

```text
crates/runtime/bundles/src/scene_reset.rs
```

To trzeba rozłożyć per plugin.

## P1.1 — dodać helper do rejestrowania reset handlerów

### MODIFY `crates/engine/scene/src/reset.rs`

Obecny plik:

```rust
5  pub trait SceneResetHandler
12 pub struct SceneResetHandlerRegistry
21 pub fn register<H>(&self, handler: H)
31 pub fn reset_all(&self, runtime: &Runtime)
```

Dodać import:

```rust
use amigo_runtime::ServiceRegistry;
```

Dodać na końcu pliku:

```rust
pub fn register_scene_reset_handler<H>(
    registry: &ServiceRegistry,
    handler: H,
) -> AmigoResult<()>
where
    H: SceneResetHandler + 'static,
{
    if let Some(reset_registry) = registry.resolve::<SceneResetHandlerRegistry>() {
        reset_registry.register(handler);
    }
    Ok(())
}
```

Opcjonalnie zmienić `expect("scene reset handler registry should not be poisoned")` na łagodniejszą obsługę, ale nie blokować refaktoru.

### Dlaczego

Każdy plugin będzie mógł zrobić:

```rust
amigo_scene::register_scene_reset_handler(registry, MyPluginSceneResetHandler)?;
```

bez ręcznego `resolve`.

---

## P1.2 — dodać reset handler do sprite plugin

### ADD `plugins/gfx/sprite-2d/src/sprite/reset.rs`

```rust
use amigo_core::AmigoResult;
use amigo_runtime::Runtime;
use amigo_scene::SceneResetHandler;

use super::SpriteSceneService;

pub struct Sprite2dSceneResetHandler;

impl SceneResetHandler for Sprite2dSceneResetHandler {
    fn name(&self) -> &'static str {
        "amigo.gfx.sprite-2d"
    }

    fn reset_scene(&self, runtime: &Runtime) -> AmigoResult<()> {
        if let Some(service) = runtime.resolve::<SpriteSceneService>() {
            service.clear();
        }
        Ok(())
    }
}
```

### MODIFY `plugins/gfx/sprite-2d/src/sprite/mod.rs`

Dodać:

```rust
mod reset;
pub use reset::*;
```

Najlepiej obok:

```rust
mod render_extraction;
mod runtime_capabilities;
```

### MODIFY `plugins/gfx/sprite-2d/src/sprite/plugin.rs`

W `impl RuntimePlugin for SpritePlugin`, funkcja `register`, po:

```rust
20 registry.register(SpriteSceneService::default())?;
```

dodać:

```rust
amigo_scene::register_scene_reset_handler(registry, super::reset::Sprite2dSceneResetHandler)?;
```

Jeżeli `super::reset` nie jest dostępne przez moduł, użyć:

```rust
crate::Sprite2dSceneResetHandler
```

### Walidacja

```bash
cargo check -p amigo-sprite-2d-plugin
```

---

## P1.3 — dodać reset handler do layered image

### ADD `plugins/gfx/layered-image-2d/src/layered_image/reset.rs`

```rust
use amigo_core::AmigoResult;
use amigo_runtime::Runtime;
use amigo_scene::SceneResetHandler;

use super::LayeredImageSceneService;

pub struct LayeredImage2dSceneResetHandler;

impl SceneResetHandler for LayeredImage2dSceneResetHandler {
    fn name(&self) -> &'static str {
        "amigo.gfx.layered-image-2d"
    }

    fn reset_scene(&self, runtime: &Runtime) -> AmigoResult<()> {
        if let Some(service) = runtime.resolve::<LayeredImageSceneService>() {
            service.clear();
        }
        Ok(())
    }
}
```

### MODIFY `plugins/gfx/layered-image-2d/src/layered_image/mod.rs`

Dodać:

```rust
mod reset;
pub use reset::*;
```

### MODIFY `plugins/gfx/layered-image-2d/src/layered_image/plugin.rs`

Po rejestracji `LayeredImageSceneService` dodać:

```rust
amigo_scene::register_scene_reset_handler(
    registry,
    super::reset::LayeredImage2dSceneResetHandler,
)?;
```

---

## P1.4 — dodać reset handler do text 2D

### ADD `plugins/gfx/text-2d/src/text/reset.rs`

```rust
use amigo_core::AmigoResult;
use amigo_runtime::Runtime;
use amigo_scene::SceneResetHandler;

use super::Text2dSceneService;

pub struct Text2dSceneResetHandler;

impl SceneResetHandler for Text2dSceneResetHandler {
    fn name(&self) -> &'static str {
        "amigo.gfx.text-2d"
    }

    fn reset_scene(&self, runtime: &Runtime) -> AmigoResult<()> {
        if let Some(service) = runtime.resolve::<Text2dSceneService>() {
            service.clear();
        }
        Ok(())
    }
}
```

### MODIFY `plugins/gfx/text-2d/src/text/mod.rs`

Na górze, obok:

```rust
mod render_extraction;
mod runtime_capabilities;
mod scene_command;
```

dodać:

```rust
mod reset;
pub use reset::*;
```

### MODIFY `plugins/gfx/text-2d/src/text/mod.rs`

W implementacji `RuntimePlugin` dla text pluginu znaleźć `registry.register(Text2dSceneService::default())?;`.

Po tej linii dodać:

```rust
amigo_scene::register_scene_reset_handler(registry, Text2dSceneResetHandler)?;
```

Jeżeli plugin jest w innym pliku niż `mod.rs`, użyć tam.

---

## P1.5 — dodać reset handler do tilemap

### ADD `plugins/gfx/tilemap-2d/src/tilemap/reset.rs`

```rust
use amigo_core::AmigoResult;
use amigo_runtime::Runtime;
use amigo_scene::SceneResetHandler;

use super::TileMap2dSceneService;

pub struct TileMap2dSceneResetHandler;

impl SceneResetHandler for TileMap2dSceneResetHandler {
    fn name(&self) -> &'static str {
        "amigo.gfx.tilemap-2d"
    }

    fn reset_scene(&self, runtime: &Runtime) -> AmigoResult<()> {
        if let Some(service) = runtime.resolve::<TileMap2dSceneService>() {
            service.clear();
        }
        Ok(())
    }
}
```

### MODIFY `plugins/gfx/tilemap-2d/src/tilemap/mod.rs`

Dodać:

```rust
mod reset;
pub use reset::*;
```

### MODIFY `plugins/gfx/tilemap-2d/src/tilemap/plugin.rs`

Po rejestracji `TileMap2dSceneService`:

```rust
amigo_scene::register_scene_reset_handler(registry, super::reset::TileMap2dSceneResetHandler)?;
```

---

## P1.6 — dodać reset handler do vector 2D

### ADD `plugins/gfx/vector-2d/src/vector/reset.rs`

```rust
use amigo_core::AmigoResult;
use amigo_runtime::Runtime;
use amigo_scene::SceneResetHandler;

use super::VectorSceneService;

pub struct Vector2dSceneResetHandler;

impl SceneResetHandler for Vector2dSceneResetHandler {
    fn name(&self) -> &'static str {
        "amigo.gfx.vector-2d"
    }

    fn reset_scene(&self, runtime: &Runtime) -> AmigoResult<()> {
        if let Some(service) = runtime.resolve::<VectorSceneService>() {
            service.clear();
        }
        Ok(())
    }
}
```

### MODIFY `plugins/gfx/vector-2d/src/vector/mod.rs`

Dodać:

```rust
mod reset;
pub use reset::*;
```

### MODIFY `plugins/gfx/vector-2d/src/vector/plugin.rs`

Po rejestracji `VectorSceneService`:

```rust
amigo_scene::register_scene_reset_handler(registry, super::reset::Vector2dSceneResetHandler)?;
```

---

## P1.7 — dodać reset handler do lighting 2D

### ADD `plugins/lighting/light-2d/src/lighting/reset.rs`

```rust
use amigo_core::AmigoResult;
use amigo_runtime::Runtime;
use amigo_scene::SceneResetHandler;

use super::{
    GlobalLight2dSceneService,
    LightGroup2dSceneService,
    LightMap2dSceneService,
};

pub struct Lighting2dSceneResetHandler;

impl SceneResetHandler for Lighting2dSceneResetHandler {
    fn name(&self) -> &'static str {
        "amigo.lighting.light-2d"
    }

    fn reset_scene(&self, runtime: &Runtime) -> AmigoResult<()> {
        if let Some(service) = runtime.resolve::<GlobalLight2dSceneService>() {
            service.clear();
        }
        if let Some(service) = runtime.resolve::<LightMap2dSceneService>() {
            service.clear();
        }
        if let Some(service) = runtime.resolve::<LightGroup2dSceneService>() {
            service.clear();
        }
        Ok(())
    }
}
```

### MODIFY `plugins/lighting/light-2d/src/lighting/mod.rs`

Dodać:

```rust
mod reset;
pub use reset::*;
```

### MODIFY `plugins/lighting/light-2d/src/lighting/plugin.rs`

Po rejestracji usług:

```rust
amigo_scene::register_scene_reset_handler(registry, super::reset::Lighting2dSceneResetHandler)?;
```

---

## P1.8 — dodać reset handler do beacon light

### ADD `plugins/lighting/beacon-light-2d/src/beacon/reset.rs`

```rust
use amigo_core::AmigoResult;
use amigo_runtime::Runtime;
use amigo_scene::SceneResetHandler;

use super::BeaconLight2dSceneService;

pub struct BeaconLight2dSceneResetHandler;

impl SceneResetHandler for BeaconLight2dSceneResetHandler {
    fn name(&self) -> &'static str {
        "amigo.lighting.beacon-light-2d"
    }

    fn reset_scene(&self, runtime: &Runtime) -> AmigoResult<()> {
        if let Some(service) = runtime.resolve::<BeaconLight2dSceneService>() {
            service.clear();
        }
        Ok(())
    }
}
```

### MODIFY `plugins/lighting/beacon-light-2d/src/beacon/mod.rs`

Dodać:

```rust
mod reset;
pub use reset::*;
```

### MODIFY `plugins/lighting/beacon-light-2d/src/beacon/plugin.rs`

Po rejestracji service:

```rust
amigo_scene::register_scene_reset_handler(registry, super::reset::BeaconLight2dSceneResetHandler)?;
```

---

## P1.9 — dodać reset handler do particles

### ADD `plugins/vfx/particles-2d/src/reset.rs`

```rust
use amigo_core::AmigoResult;
use amigo_runtime::Runtime;
use amigo_scene::SceneResetHandler;

use crate::Particle2dSceneService;

pub struct Particle2dSceneResetHandler;

impl SceneResetHandler for Particle2dSceneResetHandler {
    fn name(&self) -> &'static str {
        "amigo.vfx.particles-2d"
    }

    fn reset_scene(&self, runtime: &Runtime) -> AmigoResult<()> {
        if let Some(service) = runtime.resolve::<Particle2dSceneService>() {
            service.clear();
        }
        Ok(())
    }
}
```

### MODIFY `plugins/vfx/particles-2d/src/lib.rs`

Dodać:

```rust
mod reset;
pub use reset::*;
```

### MODIFY `plugins/vfx/particles-2d/src/plugin.rs`

Po rejestracji `Particle2dSceneService`:

```rust
amigo_scene::register_scene_reset_handler(registry, crate::Particle2dSceneResetHandler)?;
```

---

## P1.10 — dodać reset handler do camera-core

### ADD `plugins/camera/camera-core/src/runtime/reset.rs`

```rust
use amigo_core::AmigoResult;
use amigo_runtime::Runtime;
use amigo_scene::SceneResetHandler;

use crate::{
    CameraFollow2dSceneService,
    CameraService,
    Parallax2dSceneService,
};

pub struct CameraCoreSceneResetHandler;

impl SceneResetHandler for CameraCoreSceneResetHandler {
    fn name(&self) -> &'static str {
        "amigo.camera.camera-core"
    }

    fn reset_scene(&self, runtime: &Runtime) -> AmigoResult<()> {
        if let Some(service) = runtime.resolve::<CameraService>() {
            service.clear();
        }
        if let Some(service) = runtime.resolve::<CameraFollow2dSceneService>() {
            service.clear();
        }
        if let Some(service) = runtime.resolve::<Parallax2dSceneService>() {
            service.clear();
        }
        Ok(())
    }
}
```

### MODIFY `plugins/camera/camera-core/src/runtime/mod.rs`

Dodać:

```rust
pub mod reset;
pub use reset::*;
```

### MODIFY `plugins/camera/camera-core/src/runtime/plugin_runtime.rs`

W `CameraPlugin::register`, po rejestracji:

```rust
22 registry.register(CameraService::default())?;
23 registry.register(CameraFocusTarget2dService::default())?;
24 registry.register(CameraFollow2dSceneService::default())?;
25 registry.register(Parallax2dSceneService::default())?;
```

dodać:

```rust
amigo_scene::register_scene_reset_handler(registry, crate::runtime::CameraCoreSceneResetHandler)?;
```

---

## P1.11 — dodać reset handler do shutter-motion

### ADD `plugins/camera/shutter-motion/src/motion/reset.rs`

```rust
use amigo_core::AmigoResult;
use amigo_runtime::Runtime;
use amigo_scene::SceneResetHandler;

use super::Motion2dSceneService;

pub struct Motion2dSceneResetHandler;

impl SceneResetHandler for Motion2dSceneResetHandler {
    fn name(&self) -> &'static str {
        "amigo.camera.shutter-motion"
    }

    fn reset_scene(&self, runtime: &Runtime) -> AmigoResult<()> {
        if let Some(service) = runtime.resolve::<Motion2dSceneService>() {
            service.clear();
        }
        Ok(())
    }
}
```

### MODIFY `plugins/camera/shutter-motion/src/motion/mod.rs`

Dodać:

```rust
mod reset;
pub use reset::*;
```

### MODIFY `plugins/camera/shutter-motion/src/motion/plugin.rs`

Po:

```rust
73 registry.register(Motion2dSceneService::default())?;
```

dodać:

```rust
amigo_scene::register_scene_reset_handler(registry, super::reset::Motion2dSceneResetHandler)?;
```

---

## P1.12 — dodać reset handler do composition 2D

### ADD `crates/2d/composition/src/reset.rs`

```rust
use amigo_core::AmigoResult;
use amigo_runtime::Runtime;
use amigo_scene::SceneResetHandler;

use crate::{
    LightRoute2dSceneService,
    RenderLayer2dSceneService,
};

pub struct Composition2dSceneResetHandler;

impl SceneResetHandler for Composition2dSceneResetHandler {
    fn name(&self) -> &'static str {
        "amigo.2d.composition"
    }

    fn reset_scene(&self, runtime: &Runtime) -> AmigoResult<()> {
        if let Some(service) = runtime.resolve::<RenderLayer2dSceneService>() {
            service.clear();
        }
        if let Some(service) = runtime.resolve::<LightRoute2dSceneService>() {
            service.clear();
        }
        Ok(())
    }
}
```

### MODIFY `crates/2d/composition/src/lib.rs`

Dodać:

```rust
mod reset;
pub use reset::*;
```

### MODIFY `crates/2d/composition/src/plugin.rs`

Po rejestracji services:

```rust
amigo_scene::register_scene_reset_handler(registry, crate::Composition2dSceneResetHandler)?;
```

---

## P1.13 — dodać reset handler do postfx composite

### ADD `plugins/postfx/composite/src/reset.rs`

```rust
use amigo_core::AmigoResult;
use amigo_runtime::Runtime;
use amigo_scene::SceneResetHandler;

use crate::PostFx2dService;

pub struct PostFx2dSceneResetHandler;

impl SceneResetHandler for PostFx2dSceneResetHandler {
    fn name(&self) -> &'static str {
        "amigo.postfx.composite"
    }

    fn reset_scene(&self, runtime: &Runtime) -> AmigoResult<()> {
        if let Some(service) = runtime.resolve::<PostFx2dService>() {
            service.clear_scoped_stacks();
            service.set_lens_certification_reports(Vec::new());
            service.set_renderer_mode("none");
        }
        Ok(())
    }
}
```

### MODIFY `plugins/postfx/composite/src/lib.rs`

Dodać:

```rust
mod reset;
pub use reset::*;
```

### MODIFY `plugins/postfx/composite/src/plugin.rs`

Po:

```rust
48 registry.register(PostFx2dService::default())?;
```

dodać:

```rust
amigo_scene::register_scene_reset_handler(registry, crate::PostFx2dSceneResetHandler)?;
```

---

## P1.14 — przenieść pozostałe reset handlers

Dodać analogicznie:

| Obszar         | ADD file                                    | Service do czyszczenia                                                          |
| -------------- | ------------------------------------------- | ------------------------------------------------------------------------------- |
| Physics        | `crates/2d/physics/src/reset.rs`            | `Physics2dSceneService::clear()`                                                |
| UI             | `crates/ui/core/src/reset.rs`               | `UiSceneService`, `UiStateService`, `UiModelBindingService`, `UiThemeService`   |
| Audio API      | `crates/audio/api/src/reset.rs`             | `AudioSceneService`, `AudioStateService`                                        |
| Audio mixer    | `crates/audio/mixer/src/reset.rs`           | `AudioMixerService::clear()`                                                    |
| Audio output   | `crates/audio/output/src/reset.rs`          | `AudioOutputBackendService::clear_buffer()`                                     |
| State          | `crates/engine/state/src/reset.rs`          | `SceneStateService::clear_scene_defaults()`, `SceneTimerService::reset_scene()` |
| Event pipeline | `crates/engine/event-pipeline/src/reset.rs` | `EventPipelineService::clear()`                                                 |
| Behavior       | `crates/engine/behavior/src/reset.rs`       | `BehaviorSceneService::clear()`                                                 |
| Scripting API  | `crates/scripting/api/src/reset.rs`         | `ScriptComponentService::clear()`, `ScriptTraceService::clear()`                |
| 3D mesh        | `crates/3d/mesh/src/reset.rs`               | `MeshSceneService::clear()`                                                     |
| 3D text        | `crates/3d/text/src/reset.rs`               | `Text3dSceneService::clear()`                                                   |
| 3D material    | `crates/3d/material/src/reset.rs`           | `MaterialSceneService::clear()`                                                 |

Każdy plugin/crate musi w swoim `plugin.rs` zarejestrować handler przez:

```rust
amigo_scene::register_scene_reset_handler(registry, XSceneResetHandler)?;
```

---

## P1.15 — usunąć centralny reset z runtime bundles

### MODIFY `crates/runtime/bundles/src/scene_reset.rs`

Po przeniesieniu wszystkich resetów do pluginów usunąć całą strukturę:

```rust
5  pub struct FullRuntimeSceneResetHandler;
7  impl SceneResetHandler for FullRuntimeSceneResetHandler
143 pub fn register_full_runtime_scene_reset_handlers
```

Najlepiej cały plik usunąć.

### DELETE

```text
crates/runtime/bundles/src/scene_reset.rs
```

### MODIFY `crates/runtime/bundles/src/lib.rs`

Usunąć:

```rust
8  mod scene_reset;
23 pub use scene_reset::*;
```

### MODIFY `crates/apps/app/src/bootstrap.rs`

Usunąć linię:

```rust
83 amigo_runtime_bundles::register_full_runtime_scene_reset_handlers(session.runtime())?;
```

### Walidacja

```bash
rg "FullRuntimeSceneResetHandler" crates plugins
# expected: no output

rg "register_full_runtime_scene_reset_handlers" crates plugins
# expected: no output

cargo check -p amigo-runtime-bundles
cargo check -p amigo-app
```

### Dlaczego

To usuwa największy pozostały centralny katalog resetu. Po tym reset staje się naprawdę pluginowy.

---

# Priorytet 2 — rozbić `component_metadata.rs`

Obecnie:

```text
crates/engine/scene/src/component_metadata.rs
```

ma około 4151 linii i nadal zna wszystkie komponenty.

## P2.1 — dodać dynamiczny `ComponentKindId`

### MODIFY `crates/engine/scene/src/component_metadata.rs`

Po `ComponentKind` dodać:

```rust
#[derive(Debug, Clone, PartialEq, Eq, PartialOrd, Ord, Hash, Serialize, Deserialize)]
pub struct ComponentKindId(String);

impl ComponentKindId {
    pub fn new(value: impl Into<String>) -> Self {
        Self(value.into())
    }

    pub fn as_str(&self) -> &str {
        &self.0
    }
}

impl From<ComponentKind> for ComponentKindId {
    fn from(kind: ComponentKind) -> Self {
        Self::new(kind.type_name())
    }
}
```

Dodać do `impl ComponentKind`:

```rust
pub fn type_name(self) -> &'static str {
    match self {
        ComponentKind::Camera2D => "Camera2D",
        ComponentKind::Camera3D => "Camera3D",
        ComponentKind::Light3D => "Light3D",
        ComponentKind::Sprite2D => "Sprite2D",
        ComponentKind::LayeredImage2D => "LayeredImage2D",
        ComponentKind::DepthMap2D => "DepthMap2D",
        ComponentKind::DepthAuxMap2D => "DepthAuxMap2D",
        ComponentKind::GlobalLight2D => "GlobalLight2D",
        ComponentKind::LightMap2DSource => "LightMap2DSource",
        ComponentKind::TileMap2D => "TileMap2D",
        ComponentKind::Text2D => "Text2D",
        ComponentKind::VectorShape2D => "VectorShape2D",
        ComponentKind::BeaconLight2D => "BeaconLight2D",
        ComponentKind::EntityPool => "EntityPool",
        ComponentKind::Lifetime => "Lifetime",
        ComponentKind::ProjectileEmitter2D => "ProjectileEmitter2D",
        ComponentKind::InputActionMap => "InputActionMap",
        ComponentKind::Behavior => "Behavior",
        ComponentKind::EventPipeline => "EventPipeline",
        ComponentKind::UiModelBindings => "UiModelBindings",
        ComponentKind::ScriptComponent => "ScriptComponent",
        ComponentKind::ParticleEmitter2D => "ParticleEmitter2D",
        ComponentKind::Velocity2D => "Velocity2D",
        ComponentKind::Bounds2D => "Bounds2D",
        ComponentKind::FreeflightMotion2D => "FreeflightMotion2D",
        ComponentKind::KinematicBody2D => "KinematicBody2D",
        ComponentKind::AabbCollider2D => "AabbCollider2D",
        ComponentKind::StaticCollider2D => "StaticCollider2D",
        ComponentKind::CircleCollider2D => "CircleCollider2D",
        ComponentKind::Trigger2D => "Trigger2D",
        ComponentKind::MotionController2D => "MotionController2D",
        ComponentKind::CameraFollow2D => "CameraFollow2D",
        ComponentKind::Parallax2D => "Parallax2D",
        ComponentKind::TileMapMarker2D => "TileMapMarker2D",
        ComponentKind::Mesh3D => "Mesh3D",
        ComponentKind::Material3D => "Material3D",
        ComponentKind::Text3D => "Text3D",
        ComponentKind::UiDocument => "UiDocument",
        ComponentKind::UiThemeSet => "UiThemeSet",
    }
}
```

### Dlaczego

Obecny `ComponentRegistry` jest zamknięty przez:

```rust
descriptors: BTreeMap<ComponentKind, ComponentTypeDescriptor>
```

Dopóki key jest enumem, pluginy nie mogą dodawać nowych component kinds bez edycji scene core.

---

## P2.2 — zmienić `ComponentRegistry` key na `ComponentKindId`

### MODIFY `crates/engine/scene/src/component_metadata.rs`

Zmienić:

```rust
538 pub struct ComponentRegistry {
539     descriptors: BTreeMap<ComponentKind, ComponentTypeDescriptor>,
}
```

na:

```rust
pub struct ComponentRegistry {
    descriptors: BTreeMap<ComponentKindId, ComponentTypeDescriptor>,
}
```

W `ComponentRegistry::new` zmienić mapowanie:

```rust
.map(|descriptor| (descriptor.kind.into(), descriptor))
```

na:

```rust
.map(|descriptor| (ComponentKindId::from(descriptor.kind), descriptor))
```

Zostawić stare API:

```rust
pub fn descriptor(&self, kind: ComponentKind) -> Option<&ComponentTypeDescriptor> {
    self.descriptors.get(&ComponentKindId::from(kind))
}
```

Dodać nowe API:

```rust
pub fn descriptor_by_kind_id(
    &self,
    kind_id: &ComponentKindId,
) -> Option<&ComponentTypeDescriptor> {
    self.descriptors.get(kind_id)
}

pub fn descriptor_by_type_name(&self, type_name: &str) -> Option<&ComponentTypeDescriptor> {
    self.descriptors
        .values()
        .find(|descriptor| descriptor.type_name.eq_ignore_ascii_case(type_name))
}
```

W `insert`:

```rust
self.descriptors.insert(ComponentKindId::from(descriptor.kind), descriptor)
```

### Dlaczego

To umożliwia migrację do plugin-provided metadata bez natychmiastowego kasowania `ComponentKind`.

---

## P2.3 — rozszerzyć provider trait

### MODIFY `crates/engine/scene/src/component_metadata_provider.rs`

Obecnie:

```rust
3 pub trait ComponentMetadataProvider
4 fn provider_id
6 fn register_component_metadata
```

Zostawić, ale dodać registry wrapper:

```rust
use std::sync::RwLock;

#[derive(Default)]
pub struct ComponentMetadataProviderRegistry {
    providers: RwLock<Vec<Box<dyn ComponentMetadataProvider>>>,
}

impl ComponentMetadataProviderRegistry {
    pub fn register<P>(&self, provider: P)
    where
        P: ComponentMetadataProvider + 'static,
    {
        self.providers
            .write()
            .expect("component metadata provider registry poisoned")
            .push(Box::new(provider));
    }

    pub fn apply_all(&self, registry: &mut ComponentRegistry) {
        let providers = self
            .providers
            .read()
            .expect("component metadata provider registry poisoned");
        for provider in providers.iter() {
            provider.register_component_metadata(registry);
        }
    }

    pub fn provider_ids(&self) -> Vec<&'static str> {
        self.providers
            .read()
            .expect("component metadata provider registry poisoned")
            .iter()
            .map(|provider| provider.provider_id())
            .collect()
    }
}
```

### MODIFY `crates/engine/scene/src/plugin.rs`

Po:

```rust
21 registry.register(SceneResetHandlerRegistry::default())?;
```

dodać:

```rust
registry.register(ComponentMetadataProviderRegistry::default())?;
```

### MODIFY `crates/engine/scene/src/lib.rs`

Upewnić się, że eksportuje:

```rust
pub mod component_metadata_provider;
pub use component_metadata_provider::*;
```

---

## P2.4 — dodać funkcję budowania registry z providerami

### MODIFY `crates/engine/scene/src/component_metadata.rs`

Dodać:

```rust
pub fn component_registry_with_providers(
    providers: Option<&crate::ComponentMetadataProviderRegistry>,
) -> ComponentRegistry {
    let mut registry = default_component_registry();
    if let Some(providers) = providers {
        providers.apply_all(&mut registry);
    }
    registry
}
```

### MODIFY wszystkie miejsca używające `default_component_registry()`

Znaleźć:

```bash
rg "default_component_registry" crates plugins
```

Jeżeli miejsce ma dostęp do `Runtime`, zmienić:

```rust
let registry = default_component_registry();
```

na:

```rust
let providers = runtime.resolve::<amigo_scene::ComponentMetadataProviderRegistry>();
let registry = amigo_scene::component_registry_with_providers(providers.as_deref());
```

Jeżeli nie ma runtime, zostawić `default_component_registry()`.

### Dlaczego

Nie trzeba migrować wszystkiego od razu. Najpierw system zaczyna uwzględniać providerów.

---

## P2.5 — przenieść metadata po jednej domenie

Nie robić wszystkiego naraz. Kolejność:

```text
1. Sprite2D
2. LayeredImage2D
3. Text2D
4. VectorShape2D
5. TileMap2D
6. ParticleEmitter2D
7. Camera2D / CameraFollow2D / Parallax2D
8. Light2D / LightMap / Beacon
9. UI
10. Physics
```

Dla każdej domeny:

### ADD przykład dla sprite

```text
plugins/gfx/sprite-2d/src/scene/metadata.rs
```

```rust
use amigo_scene::{
    ComponentMetadataProvider,
    ComponentRegistry,
    sprite_2d_descriptor,
};

pub struct Sprite2dComponentMetadataProvider;

impl ComponentMetadataProvider for Sprite2dComponentMetadataProvider {
    fn provider_id(&self) -> &'static str {
        "amigo.gfx.sprite-2d"
    }

    fn register_component_metadata(&self, registry: &mut ComponentRegistry) {
        registry.insert(sprite_2d_descriptor());
    }
}
```

### MODIFY `plugins/gfx/sprite-2d/src/scene/mod.rs`

Dodać:

```rust
mod metadata;
pub use metadata::*;
```

### MODIFY `plugins/gfx/sprite-2d/src/sprite/plugin.rs`

Po rejestracji service:

```rust
if let Some(metadata) = registry.resolve::<amigo_scene::ComponentMetadataProviderRegistry>() {
    metadata.register(crate::scene::Sprite2dComponentMetadataProvider);
}
```

### MODIFY `crates/engine/scene/src/component_metadata.rs`

Po migracji sprite usunąć z `default_component_registry()`:

```rust
sprite_2d_descriptor(),
```

Nie usuwać funkcji `sprite_2d_descriptor()` od razu, bo provider dalej ją używa. Później przenieść samą funkcję do pluginu.

### Walidacja per domena

```bash
cargo check -p amigo-scene
cargo check -p amigo-sprite-2d-plugin
rg "sprite_2d_descriptor\\(\\)" crates/engine/scene/src/component_metadata.rs
```

Docelowo po pełnej migracji:

```text
default_component_registry() powinno zawierać tylko naprawdę core scene components.
```

---

# Priorytet 3 — dokończyć devtools provider model

## P3.1 — usunąć martwe command files z devtools

W `crates/engine/devtools/src/commands/mod.rs` nie są podpięte:

```text
commands/layered.rs
commands/lighting.rs
commands/particles.rs
commands/postfx.rs
```

One nadal importują pluginy, ale `devtools/Cargo.toml` już nie ma plugin deps. To są migracyjne resztki.

### DELETE

```text
crates/engine/devtools/src/commands/layered.rs
crates/engine/devtools/src/commands/lighting.rs
crates/engine/devtools/src/commands/particles.rs
crates/engine/devtools/src/commands/postfx.rs
```

### Walidacja

```bash
rg "amigo_layered_image_2d_plugin|amigo_light_2d_plugin|amigo_particles_2d_plugin|amigo_composite_plugin" crates/engine/devtools/src
# expected: no output

cargo check -p amigo-devtools
```

### Dlaczego

Devtools core nie może zawierać plugin-specific commands. Te komendy są już rejestrowane przez pluginy, np. `PostFx2dPlugin` w `plugins/postfx/composite/src/plugin.rs`.

---

## P3.2 — rozbić `completion.rs`

Obecnie:

```text
crates/engine/devtools/src/completion.rs
```

ma około 1985 linii.

### MOVE

Stworzyć katalog:

```text
crates/engine/devtools/src/completion/
```

Przenieść obecny `completion/provider.rs` do:

```text
crates/engine/devtools/src/completion/provider.rs
```

Przenieść zawartość `completion.rs` do modułów:

```text
crates/engine/devtools/src/completion/mod.rs
crates/engine/devtools/src/completion/model.rs
crates/engine/devtools/src/completion/state.rs
crates/engine/devtools/src/completion/rhai.rs
crates/engine/devtools/src/completion/command.rs
crates/engine/devtools/src/completion/resources.rs
```

### Docelowy podział

`model.rs`:

```text
ConsoleCompletionKind
ConsoleCompletionSuggestion
ConsoleRhaiValueKind
ConsoleRhaiSymbol
ConsoleCompletionContext
ConsoleCompletionEdit
ConsoleCompletionSnapshot
```

`state.rs`:

```text
ConsoleCompletionState
completion snapshot storage
apply_suggestion
```

`rhai.rs`:

```text
builtin_rhai_symbols
collect_console_rhai_symbols_from_source
declared_rhai_symbol_from_statement
infer_rhai_value_kind_from_expression
rhai_properties_for_value_kind
```

`command.rs`:

```text
command name/alias/arg completion logic
ConsoleCommandSchema completion
```

`resources.rs`:

```text
entity_names
render_layer_ids
postfx indices/kinds
runtime_control completions
```

`provider.rs`:

```text
ConsoleCompletionProvider
ConsoleCompletionProviderRegistry
```

### MODIFY `crates/engine/devtools/src/lib.rs`

Obecnie eksportuje z `completion.rs`. Po przeniesieniu ma działać tak samo:

```rust
pub mod completion;
pub use completion::{
    ConsoleCompletionProvider,
    ConsoleCompletionProviderRegistry,
    ConsoleCompletionState,
    ConsoleCompletionContext,
    ConsoleCompletionSuggestion,
    ConsoleCompletionSnapshot,
    ConsoleCompletionEdit,
};
```

### DELETE

Po migracji usunąć stary plik:

```text
crates/engine/devtools/src/completion.rs
```

albo zostawić jako thin wrapper:

```rust
pub mod completion;
```

Nie trzymać dwóch źródeł prawdy.

---

## P3.3 — rozszerzyć provider o symbole i properties

### MODIFY `crates/engine/devtools/src/completion/provider.rs`

Obecnie:

```rust
fn augment_context(&self, runtime, context)
```

Dodać:

```rust
fn rhai_symbols(&self, _runtime: &Runtime) -> Vec<ConsoleRhaiSymbol> {
    Vec::new()
}

fn rhai_properties(&self, _value_kind: &str) -> Option<Vec<String>> {
    None
}

fn postfx_kinds(&self, _runtime: &Runtime) -> Vec<String> {
    Vec::new()
}
```

Wymaga przeniesienia `ConsoleRhaiSymbol` importu z `model.rs`.

### MODIFY `crates/engine/devtools/src/console_input_controller.rs`

W okolicy linii 398–418:

```rust
postfx_kinds: vec![
    "blur",
    "crt",
    "dirty_bloom",
    ...
],
```

usunąć hardcoded listę.

Zastąpić:

```rust
let mut postfx_kinds = Vec::new();
if let Some(providers) = runtime.resolve::<crate::ConsoleCompletionProviderRegistry>() {
    postfx_kinds.extend(providers.postfx_kinds(runtime));
}
```

Jeżeli provider API nie ma `postfx_kinds`, dodać metodę do registry:

```rust
pub fn postfx_kinds(&self, runtime: &Runtime) -> Vec<String> {
    let providers = self.providers.read().expect(...);
    providers
        .iter()
        .flat_map(|provider| provider.postfx_kinds(runtime))
        .collect()
}
```

### MODIFY `plugins/postfx/composite/src/plugin.rs`

`PostFx2dConsoleCompletionProvider` obecnie tylko uzupełnia indices. Dodać:

```rust
fn postfx_kinds(&self, _runtime: &amigo_runtime::Runtime) -> Vec<String> {
    vec![
        "blur".to_owned(),
        "crt".to_owned(),
        "dirty_bloom".to_owned(),
        "rain_glass".to_owned(),
        "lens_droplets".to_owned(),
        "color_quantize".to_owned(),
        "film_noise".to_owned(),
        "shutter_blur".to_owned(),
    ]
}
```

### Dlaczego

Devtools nie może znać listy efektów post-FX. To wie plugin composite.

---

# Priorytet 4 — rozbić `runtime/bundles/render_extractor_bridges`

## P4.1 — rozbić `world_2d.rs` na moduły

Obecnie:

```text
crates/runtime/bundles/src/render_extractor_bridges/world_2d.rs
```

ma około 1029 linii i zawiera wszystkie bridge’e.

### ADD katalog

```text
crates/runtime/bundles/src/render_extractor_bridges/world_2d/
```

### MOVE z `world_2d.rs` do:

```text
world_2d/mod.rs
world_2d/sprite.rs
world_2d/layered_image.rs
world_2d/tilemap.rs
world_2d/text.rs
world_2d/vector.rs
world_2d/beacon.rs
world_2d/depth.rs
world_2d/composition.rs
world_2d/lighting.rs
world_2d/particles.rs
world_2d/post_fx.rs
world_2d/camera_capture.rs
world_2d/visual_maps.rs
world_2d/tests.rs
```

### DELETE

Po migracji usunąć:

```text
crates/runtime/bundles/src/render_extractor_bridges/world_2d.rs
```

### MODIFY `crates/runtime/bundles/src/render_extractor_bridges/mod.rs`

Zostawić:

```rust
mod world_2d;
```

Rust znajdzie `world_2d/mod.rs`.

### Docelowy `world_2d/mod.rs`

```rust
use super::context::WgpuRenderExtractorRegistry;

mod beacon;
mod camera_capture;
mod composition;
mod depth;
mod layered_image;
mod lighting;
mod particles;
mod post_fx;
mod sprite;
mod text;
mod tilemap;
mod vector;
mod visual_maps;

#[cfg(test)]
mod tests;

pub fn register_world_2d_render_extractors(registry: &mut WgpuRenderExtractorRegistry) {
    tilemap::register(registry);
    sprite::register(registry);
    layered_image::register(registry);
    depth::register(registry);
    vector::register(registry);
    beacon::register(registry);
    text::register(registry);
    composition::register(registry);
    lighting::register(registry);
    particles::register(registry);
    post_fx::register(registry);
}
```

### Przykład `sprite.rs`

Przenieść z obecnych linii 52–76:

```rust
use amigo_render_api::RenderFrameExtractor;
use amigo_render_wgpu::WgpuRenderFramePacket;
use amigo_runtime::Runtime;
use amigo_scene::SceneService;

use crate::render_extractor_bridges::context::WgpuRenderExtractorRegistry;

fn optional<T: Send + Sync + 'static>(runtime: &Runtime) -> Option<std::sync::Arc<T>> {
    runtime.resolve::<T>()
}

pub fn register(registry: &mut WgpuRenderExtractorRegistry) {
    registry.register(WgpuSprite2dRenderExtractorBridge);
}

pub struct WgpuSprite2dRenderExtractorBridge;

impl RenderFrameExtractor<Runtime, WgpuRenderFramePacket> for WgpuSprite2dRenderExtractorBridge {
    fn name(&self) -> &'static str {
        amigo_sprite_2d_plugin::Sprite2dRenderExtractor.name()
    }

    fn extract(&self, runtime: &Runtime, packet: &mut WgpuRenderFramePacket) {
        let Some(scene_service) = optional::<SceneService>(runtime) else {
            return;
        };
        let Some(sprite_scene_service) =
            optional::<amigo_sprite_2d_plugin::SpriteSceneService>(runtime)
        else {
            return;
        };
        amigo_sprite_2d_plugin::Sprite2dRenderExtractor.extract(
            amigo_sprite_2d_plugin::Sprite2dRenderExtractionContext {
                scene_service: scene_service.as_ref(),
                sprite_scene_service: sprite_scene_service.as_ref(),
            },
            packet,
        );
    }
}
```

### ADD common helper

```text
crates/runtime/bundles/src/render_extractor_bridges/world_2d/common.rs
```

```rust
use amigo_runtime::Runtime;
use std::sync::Arc;

pub fn optional<T: Send + Sync + 'static>(runtime: &Runtime) -> Option<Arc<T>> {
    runtime.resolve::<T>()
}
```

W modułach używać:

```rust
use super::common::optional;
```

### Dlaczego

To nie zmienia architektury jeszcze radykalnie, ale usuwa jeden centralny 1000-liniowy plik.

---

## P4.2 — wydzielić camera capture z `world_2d.rs`

Obecnie w `world_2d.rs`:

```text
326 fn update_camera_2d_capture
379 fn build_visual_source_flags_2d
432 fn build_camera_capture_input
530 fn targets_scene_highlight_buffer
537 fn targets_scene_emissive_buffer
544 fn should_produce_scene_normal
549 fn should_produce_scene_wetness
554 fn has_visual_map
561 fn first_visual_map
```

### MOVE

Do:

```text
crates/runtime/bundles/src/render_extractor_bridges/world_2d/camera_capture.rs
crates/runtime/bundles/src/render_extractor_bridges/world_2d/visual_maps.rs
```

`camera_capture.rs`:

```text
update_camera_2d_capture
build_visual_source_flags_2d
build_camera_capture_input
is_produced
```

`visual_maps.rs`:

```text
has_visual_map
first_visual_map
visual_map_for_kind
wetness_normal_source
wetness_mask_source
motion_source
should_produce_scene_normal
should_produce_scene_wetness
targets_scene_highlight_buffer
targets_scene_emissive_buffer
```

### MODIFY `post_fx.rs`

`WgpuPostFx2dRenderExtractorBridge::extract` ma wołać:

```rust
camera_capture::update_camera_2d_capture(runtime, camera_service.as_ref(), packet);
```

`update_camera_2d_capture` musi być:

```rust
pub(super) fn update_camera_2d_capture(...)
```

---

## P4.3 — rozbić `light_sources_2d.rs`

Obecnie:

```text
crates/runtime/bundles/src/render_extractor_bridges/light_sources_2d.rs
```

ma około 1164 linii.

### ADD katalog

```text
crates/runtime/bundles/src/render_extractor_bridges/light_sources_2d/
```

### MOVE do:

```text
light_sources_2d/mod.rs
light_sources_2d/beacon.rs
light_sources_2d/global.rs
light_sources_2d/lightmap.rs
light_sources_2d/light_group.rs
light_sources_2d/particles.rs
light_sources_2d/material.rs
light_sources_2d/camera_capture.rs
light_sources_2d/format.rs
light_sources_2d/roles.rs
light_sources_2d/tests.rs
```

### MODIFY `render_extractor_bridges/mod.rs`

Obecnie:

```rust
4 pub mod light_sources_2d;
```

Zostawić. Rust użyje `light_sources_2d/mod.rs`.

### Docelowy `light_sources_2d/mod.rs`

```rust
mod beacon;
mod camera_capture;
mod format;
mod global;
mod light_group;
mod lightmap;
mod material;
mod particles;
mod roles;

pub use camera_capture::{
    collect_camera_optical_candidates_from_light_sources_2d,
};
pub use format::format_light_sources_2d;

use amigo_render_wgpu::Renderable2dItem;
use amigo_render_api::LightSource2dCommon;

pub fn collect_light_sources_2d(
    renderables: &[Renderable2dItem],
    beacons: &[amigo_beacon_light_2d_plugin::BeaconLight2dDrawCommand],
    global_lights: &[amigo_light_2d_plugin::GlobalLight2dCommand],
    lightmaps: &[amigo_light_2d_plugin::LightMap2dSourceCommand],
    light_groups: &[amigo_light_2d_plugin::LightGroup2dCommand],
    particles: &[amigo_particles_2d_plugin::Particle2dDrawCommand],
    capture: Option<&amigo_render_api::CameraCaptureInput2d>,
) -> Vec<LightSource2dCommon> {
    let mut sources = Vec::new();
    beacon::collect(beacons, &mut sources);
    global::collect(global_lights, &mut sources);
    lightmap::collect(lightmaps, &mut sources);
    light_group::collect(light_groups, &mut sources);
    particles::collect(particles, &mut sources);
    material::collect(renderables, &mut sources);
    camera_capture::collect_visual_sources(capture, &mut sources);
    sources
}
```

### Zasada

Nie zmieniać semantyki w tym commicie. Tylko przenieść funkcje.

### Walidacja

```bash
cargo check -p amigo-runtime-bundles
```

---

# Priorytet 5 — odchudzić `apps/app`

Największy problem app:

```text
crates/apps/app/src/lib.rs
crates/apps/app/src/render_runtime.rs
crates/apps/app/src/render_runtime/services.rs
crates/apps/app/src/summary.rs
crates/apps/app/src/event_pipeline.rs
crates/apps/app/src/script_runtime/mod.rs
```

App nadal importuje konkretne plugin services przez `amigo_runtime_bundles::amigo_*`.

## P5.1 — przenieść render frame orchestration z app do runtime-bundles/render_session

### ADD

```text
crates/runtime/bundles/src/render_session.rs
```

### MODIFY `crates/runtime/bundles/src/lib.rs`

Dodać:

```rust
mod render_session;
pub use render_session::*;
```

### MOVE z `crates/apps/app/src/render_runtime.rs`

Przenieść do `runtime/bundles/src/render_session.rs`:

```text
CameraFocusPlanInfo
WgpuEditorOverlayOutput
extract_game_frame_packet
extract_live_host_overlay_packet
render_game_frame_to_cache
render_camera_capture_summary
render_visual_items_summary
camera_focus_for_input
render_camera_contributions_summary
render_camera_focus_plan_summary
```

Nie przenosić od razu `build_render_frame_for_session`, bo jest mocno związane z surface/renderer/app diagnostics. Najpierw wydzielić helpers.

### MODIFY `crates/apps/app/src/render_runtime.rs`

Po przeniesieniu importować:

```rust
use amigo_runtime_bundles::{
    extract_game_frame_packet,
    extract_live_host_overlay_packet,
    render_game_frame_to_cache,
};
```

albo zachować app wrappery, które tylko delegują.

### Dlaczego

`app` nie powinno znać detali camera focus summary, optical candidates formatting, visual source flags itd.

---

## P5.2 — przenieść `render_runtime/services.rs`

Obecnie `render_runtime/services.rs` buduje tymczasowe scene services z `WgpuRenderFramePacket`.

To jest backend adapter logic, nie app logic.

### MOVE

Z:

```text
crates/apps/app/src/render_runtime/services.rs
```

Do:

```text
crates/runtime/bundles/src/render_packet_services.rs
```

### MODIFY `crates/runtime/bundles/src/lib.rs`

Dodać:

```rust
mod render_packet_services;
pub use render_packet_services::*;
```

### MODIFY `crates/apps/app/src/render_runtime.rs`

Zmienić importy:

```rust
pub(crate) use services::{
    build_depth_map2d_scene_service_from_packet,
    ...
};
```

na:

```rust
use amigo_runtime_bundles::{
    build_depth_map2d_scene_service_from_packet,
    build_global_light2d_scene_service_from_packet,
    ...
};
```

### DELETE

Po migracji:

```text
crates/apps/app/src/render_runtime/services.rs
```

oraz w `render_runtime.rs` usunąć:

```rust
mod services;
```

### Dlaczego

To zmniejsza liczbę plugin imports w `apps/app`.

---

## P5.3 — przenieść frame graph builder z app do render-api albo runtime-bundles

Obecnie:

```text
crates/apps/app/src/render_runtime/graph.rs
```

To neutralny builder z `FrameCompositionPlan` do `FrameGraph`.

### MOVE lepsze miejsce

```text
crates/engine/render-api/src/frame_graph_builder.rs
```

### ADD `crates/engine/render-api/src/frame_graph_builder.rs`

Przenieść:

```rust
AppFrameGraphBuildInfo
build_frame_graph_from_plan
```

Zmienić nazwę:

```rust
pub struct FrameGraphBuildInfo {
    pub width: u32,
    pub height: u32,
}

pub fn build_frame_graph_from_plan(
    plan: &FrameCompositionPlan,
    info: FrameGraphBuildInfo,
) -> FrameGraph
```

### MODIFY `crates/engine/render-api/src/lib.rs`

Dodać:

```rust
pub mod frame_graph_builder;
pub use frame_graph_builder::*;
```

### MODIFY `crates/apps/app/src/render_runtime.rs`

Zamienić:

```rust
pub(crate) use graph::{AppFrameGraphBuildInfo, build_frame_graph_from_plan};
```

na:

```rust
use amigo_render_api::{FrameGraphBuildInfo, build_frame_graph_from_plan};
```

Zamienić:

```rust
AppFrameGraphBuildInfo { width, height }
```

na:

```rust
FrameGraphBuildInfo { width, height }
```

### DELETE

```text
crates/apps/app/src/render_runtime/graph.rs
```

oraz w `render_runtime.rs` usunąć:

```rust
mod graph;
```

### Walidacja

```bash
cargo check -p amigo-render-api
cargo check -p amigo-app
```

---

## P5.4 — usunąć plugin imports z `apps/app/src/lib.rs`

Obecnie `lib.rs` linie 28–53 importują dużo plugin services.

### MODIFY `crates/apps/app/src/lib.rs`

Usuń z globalnych importów:

```rust
use amigo_runtime_bundles::amigo_shutter_motion_plugin::Motion2dSceneService;
use amigo_runtime_bundles::amigo_particles_2d_plugin::Particle2dSceneService;
use amigo_runtime_bundles::amigo_2d_physics::Physics2dSceneService;
use amigo_runtime_bundles::amigo_sprite_2d_plugin::{SpriteSceneService, SpriteSheet};
use amigo_runtime_bundles::amigo_text_2d_plugin::Text2dSceneService;
use amigo_runtime_bundles::amigo_tilemap_2d_plugin::TileMap2dSceneService;
use amigo_runtime_bundles::amigo_vector_2d_plugin::VectorSceneService;
use amigo_runtime_bundles::amigo_3d_material::MaterialSceneService;
use amigo_runtime_bundles::amigo_3d_mesh::MeshSceneService;
use amigo_runtime_bundles::amigo_3d_text::Text3dSceneService;
use amigo_runtime_bundles::amigo_audio_api::{...};
use amigo_runtime_bundles::amigo_audio_mixer::AudioMixerService;
use amigo_runtime_bundles::amigo_audio_output::{...};
use amigo_runtime_bundles::amigo_behavior::BehaviorSceneService;
use amigo_camera_core_plugin::{CameraFollow2dSceneService, Parallax2dSceneService};
use amigo_runtime_bundles::amigo_event_pipeline::EventPipelineService;
use amigo_runtime_bundles::amigo_input_actions::InputActionService;
use amigo_runtime_bundles::amigo_ui::{...};
```

Przenieść importy lokalnie do modułów, które ich jeszcze potrzebują, albo po P5.1/P5.2 usunąć całkiem.

### Walidacja

```bash
rg "amigo_runtime_bundles::amigo_|amigo_camera_core_plugin|amigo_camera_optics_plugin" crates/apps/app/src/lib.rs
# expected: no output or only test cfg

cargo check -p amigo-app
```

---

# Priorytet 6 — uporządkować `render-wgpu/frame_packet.rs`

Obecnie:

```rust
30 pub enum Renderable2dPayload {
31     TileMap(...)
32     LayeredImage(...)
33     Vector(...)
34     Beacon(...)
35     Sprite(...)
36     Text(...)
37     Particle(...)
}
```

To jest zamknięta lista renderables.

Nie robić pełnej wymiany od razu. Najpierw dodać neutralny adapter layer.

## P6.1 — dodać neutralny `Renderable2dPayloadKind`

### MODIFY `crates/engine/render-wgpu/src/frame_packet.rs`

Po `Renderable2dPayload` dodać:

```rust
#[derive(Debug, Clone, PartialEq, Eq, PartialOrd, Ord, Hash)]
pub struct Renderable2dPayloadKind(String);

impl Renderable2dPayloadKind {
    pub fn new(value: impl Into<String>) -> Self {
        Self(value.into())
    }

    pub fn as_str(&self) -> &str {
        &self.0
    }
}
```

Dodać metodę:

```rust
impl Renderable2dPayload {
    pub fn kind_id(&self) -> Renderable2dPayloadKind {
        match self {
            Renderable2dPayload::TileMap(_) => Renderable2dPayloadKind::new("tilemap_2d"),
            Renderable2dPayload::LayeredImage(_) => Renderable2dPayloadKind::new("layered_image_2d"),
            Renderable2dPayload::Vector(_) => Renderable2dPayloadKind::new("vector_2d"),
            Renderable2dPayload::Beacon(_) => Renderable2dPayloadKind::new("beacon_light_2d"),
            Renderable2dPayload::Sprite(_) => Renderable2dPayloadKind::new("sprite_2d"),
            Renderable2dPayload::Text(_) => Renderable2dPayloadKind::new("text_2d"),
            Renderable2dPayload::Particle(_) => Renderable2dPayloadKind::new("particle_2d"),
        }
    }
}
```

### MODIFY `Renderable2dItem::payload_kind`

Obecnie:

```rust
61 pub fn payload_kind(&self) -> &'static str {
62     self.common.kind.as_str()
}
```

Zostawić, ale dodać:

```rust
pub fn payload_kind_id(&self) -> Renderable2dPayloadKind {
    self.payload.kind_id()
}
```

### Dlaczego

To etap przygotowawczy pod adapter registry bez łamania aktualnego renderer path.

---

## P6.2 — dodać adapter registry

### ADD

```text
crates/engine/render-wgpu/src/renderable_adapter.rs
```

```rust
use crate::{Renderable2dPayload, Renderable2dPayloadKind};

pub trait WgpuRenderable2dAdapter: Send + Sync {
    fn kind(&self) -> Renderable2dPayloadKind;

    fn supports(&self, payload: &Renderable2dPayload) -> bool {
        payload.kind_id() == self.kind()
    }
}

#[derive(Default)]
pub struct WgpuRenderable2dAdapterRegistry {
    adapters: Vec<Box<dyn WgpuRenderable2dAdapter>>,
}

impl WgpuRenderable2dAdapterRegistry {
    pub fn register<A>(&mut self, adapter: A)
    where
        A: WgpuRenderable2dAdapter + 'static,
    {
        self.adapters.push(Box::new(adapter));
    }

    pub fn len(&self) -> usize {
        self.adapters.len()
    }
}
```

### MODIFY `crates/engine/render-wgpu/src/lib.rs`

Dodać:

```rust
mod renderable_adapter;
pub use renderable_adapter::*;
```

### Następny krok

Dopiero później przenieść konkretne render paths do adapterów:

```text
sprite adapter
text adapter
vector adapter
tilemap adapter
layered image adapter
particle adapter
beacon adapter
```

Nie robić w tym samym commicie.

---

# Priorytet 7 — camera debug views: usunąć relight special-case z render-api

Obecnie `render-api/src/camera_capture.rs` zawiera `relight.plate.*` aliases w `CameraDebugView2d::parse`, linie 200–250.

To jest plugin-specific i nie powinno siedzieć w render-api.

## P7.1 — przenieść parsing do `engine/camera`

### MODIFY `crates/engine/camera/src/debug_view.rs`

Rozszerzyć `CameraDebugViewId::parse` o wszystkie camera core aliasy:

```rust
"computed_z_depth" | "z_depth" | "computed_depth" => Self::new("camera.computed_z_depth"),
"layer_optical_roles" | "roles" => Self::new("camera.layer_optical_roles"),
"layer_mask" | "mask" => Self::new("camera.layer_mask"),
"scene_wetness" | "wetness" => Self::new("camera.scene_wetness"),
"camera_after_exposure" | "after_exposure" => Self::new("camera.after_exposure"),
...
```

Nie dodawać tutaj `relight.plate.*`.

### MODIFY `crates/engine/render-api/src/camera_capture.rs`

W `CameraDebugView2d::parse` zastąpić cały match liniami 173–256:

```rust
pub fn parse(value: &str) -> Self {
    Self(CameraDebugViewId::parse(value))
}
```

Usunąć wszystkie aliasy `plate_relight_*`.

### ADD `plugins/lighting/relight-2d/src/debug_view.rs`

Dodać provider/registry dla relight debug views, np.:

```rust
use amigo_camera::{CameraDebugViewDescriptor, CameraDebugViewId};

pub fn relight_debug_view_descriptors() -> Vec<CameraDebugViewDescriptor> {
    vec![
        CameraDebugViewDescriptor {
            id: CameraDebugViewId::new("relight.plate.aux_depth"),
            label: "Plate Relight Aux Depth".to_owned(),
            aliases: vec![
                "plate_relight_aux_depth".to_owned(),
                "plate_aux_depth".to_owned(),
                "relight_aux_depth".to_owned(),
            ],
            tags: vec!["relight".to_owned(), "debug".to_owned()],
            stop_after_feature: None,
        },
        // repeat for aux_height, aux_occluder, normal, contribution, shadow, etc.
    ]
}
```

### MODIFY `plugins/lighting/relight-2d/src/lib.rs`

Dodać:

```rust
mod debug_view;
pub use debug_view::*;
```

### Dlaczego

`render-api` nie może wiedzieć o `relight`. Kamera może mieć generic debug IDs, pluginy dostarczają aliases/descriptors.

---

# Priorytet 8 — `engine/behavior` nie powinno być engine core

Obecnie:

```text
crates/engine/behavior/Cargo.toml
```

zależy od:

```text
amigo-camera-core-plugin
amigo-shutter-motion-plugin
amigo-particles-2d-plugin
amigo-2d-physics
amigo-audio-api
amigo-ui
```

To wygląda jak gameplay/domain plugin, nie neutralny engine.

## P8.1 — przenieść crate

### MOVE

Z:

```text
crates/engine/behavior
```

Do:

```text
plugins/gameplay/behavior
```

albo:

```text
crates/runtime/behavior
```

Rekomendacja: `plugins/gameplay/behavior`.

### MODIFY root `Cargo.toml`

W `[workspace].members` zmienić:

```toml
"crates/engine/behavior",
```

na:

```toml
"plugins/gameplay/behavior",
```

W `[workspace.dependencies]` zmienić path:

```toml
amigo-behavior = { path = "plugins/gameplay/behavior" }
```

Nazwa crate może zostać:

```toml
name = "amigo-behavior"
```

### MODIFY dokumentacja

W `AGENTS.md` albo `arch.md` zmienić kategorię z engine core na gameplay plugin/domain.

### Dlaczego

To czyści granice. `engine/*` powinno być neutralne. `behavior` nie jest neutralne, bo zna particles/audio/camera/physics.

---

# Priorytet 9 — `editor-ingame` provider model

Obecnie:

```text
crates/engine/editor-ingame/Cargo.toml
```

zależy od:

```toml
amigo-composite-plugin
amigo-layered-image-2d-plugin
amigo-particles-2d-plugin
```

To może być tymczasowo akceptowalne, ale do 9/10 lepiej dodać provider registry.

## P9.1 — dodać editor command provider do `editor-api`

### ADD `crates/engine/editor-api/src/provider.rs`

```rust
use amigo_runtime::Runtime;

pub trait EditorCommandProvider: Send + Sync {
    fn provider_id(&self) -> &'static str;

    fn register_commands(&self, _runtime: &Runtime, _out: &mut Vec<EditorCommandDescriptor>) {}
}

#[derive(Clone, Debug)]
pub struct EditorCommandDescriptor {
    pub id: String,
    pub label: String,
    pub category: String,
}
```

### MODIFY `crates/engine/editor-api/src/lib.rs`

Dodać:

```rust
pub mod provider;
pub use provider::*;
```

### ADD `crates/engine/editor-ingame/src/provider_registry.rs`

```rust
use std::sync::RwLock;
use amigo_editor_api::EditorCommandProvider;

#[derive(Default)]
pub struct IngameEditorProviderRegistry {
    providers: RwLock<Vec<Box<dyn EditorCommandProvider>>>,
}

impl IngameEditorProviderRegistry {
    pub fn register<P>(&self, provider: P)
    where
        P: EditorCommandProvider + 'static,
    {
        self.providers.write().expect("editor provider registry poisoned").push(Box::new(provider));
    }
}
```

### MODIFY `crates/engine/editor-ingame/src/lib.rs`

Dodać:

```rust
mod provider_registry;
pub use provider_registry::*;
```

### MODIFY `crates/engine/editor-ingame/src/plugin.rs`

Zarejestrować:

```rust
if !registry.has::<IngameEditorProviderRegistry>() {
    registry.register(IngameEditorProviderRegistry::default())?;
}
```

### Następny krok

Pluginy `composite`, `layered-image`, `particles` rejestrują swoje editor providers. Potem usuwać ich bezpośrednie zależności z `editor-ingame`.

---

# Priorytet 10 — usunąć plugin-specific imports z scripting/app

To jest większy temat, ale warto go zacząć.

## P10.1 — `apps/app/src/script_runtime/mod.rs`

Obecnie app obsługuje plugin-specific script commands:

```text
layered_image
audio
ui
```

### ADD w `scripting-api`

```text
crates/scripting/api/src/script_command_provider.rs
```

```rust
use amigo_core::AmigoResult;
use amigo_runtime::Runtime;

pub trait RuntimeScriptCommandProvider: Send + Sync {
    fn provider_id(&self) -> &'static str;

    fn can_handle(&self, command: &ScriptCommand) -> bool;

    fn handle(&self, runtime: &Runtime, command: &ScriptCommand) -> AmigoResult<bool>;
}
```

Jeżeli istnieje już `RuntimeScriptCommandHandlerRegistry`, użyć jego modelu zamiast dodawania nowego.

### MODIFY pluginy

Pluginy, które obecnie są obsługiwane w app, mają same rejestrować script handlers:

```text
plugins/gfx/layered-image-2d
crates/audio/api
crates/ui/core
```

### MODIFY `apps/app/src/script_runtime/mod.rs`

Usunąć plugin-specific branches i zostawić tylko:

```rust
script_command_registry.dispatch(runtime, command)
```

### Dlaczego

App nie powinno znać skryptowych komend konkretnych domen.

---

# Priorytet 11 — cleanup po migracjach

## P11.1 — usunąć stare duplicate post-fx model files

Sprawdzić:

```bash
rg "pub use amigo_render_api" plugins/postfx/composite/src/model
rg "struct PostFx2dId|enum PostFxScope2d|PostFxPipelineKind" plugins/postfx/composite/src/model crates/engine/render-api/src
```

Jeżeli pliki w `plugins/postfx/composite/src/model/*` tylko re-exportują z `render-api` albo są martwe, usunąć duplikaty. Jedno źródło prawdy ma być:

```text
crates/engine/render-api/src/post_fx_model/*
```

Plugin może re-exportować:

```rust
pub use amigo_render_api::post_fx_model::*;
```

---

## P11.2 — ograniczyć `#[allow]`

Komenda:

```bash
rg "#\[allow\(" --glob '!**/tests/**' --glob '!**/*tests.rs'
```

Dla każdego:

```text
dead_code -> usunąć albo feature-gate
too_many_arguments -> context struct
type_complexity -> named type
unused -> usunąć import/plik
```

Target:

```text
mniej niż 20 allow poza testami
```

---

## P11.3 — sprawdzić złe zależności

Po refaktorach:

```bash
cargo tree -p amigo-scene --prefix none | rg "plugin"
# expected: no output

cargo tree -p amigo-render-api --prefix none | rg "plugin"
# expected: no output

cargo tree -p amigo-devtools --prefix none | rg "render-wgpu|plugin"
# expected: no output

cargo tree -p amigo-app --prefix none | rg "amigo-.*-plugin"
# expected: minimal / ideally no direct plugin deps except selected bundle/backend
```

---

# Finalna kolejność commitów

## Commit 1 — workspace/tools sanity

```text
MODIFY Cargo.toml
VALIDATE cargo metadata --no-deps
```

## Commit 2 — scene reset helper

```text
MODIFY crates/engine/scene/src/reset.rs
VALIDATE cargo check -p amigo-scene
```

## Commit 3 — reset handlers in major 2D plugins

```text
ADD/MODIFY sprite reset
ADD/MODIFY layered image reset
ADD/MODIFY text reset
ADD/MODIFY tilemap reset
ADD/MODIFY vector reset
VALIDATE cargo check per plugin
```

## Commit 4 — reset handlers in lighting/camera/postfx/particles

```text
ADD/MODIFY light reset
ADD/MODIFY beacon reset
ADD/MODIFY particles reset
ADD/MODIFY camera-core reset
ADD/MODIFY shutter-motion reset
ADD/MODIFY postfx reset
VALIDATE cargo check targeted
```

## Commit 5 — reset handlers in core domains

```text
ADD/MODIFY composition reset
ADD/MODIFY physics reset
ADD/MODIFY ui reset
ADD/MODIFY audio reset
ADD/MODIFY state reset
ADD/MODIFY event-pipeline reset
ADD/MODIFY behavior reset
ADD/MODIFY scripting reset
```

## Commit 6 — remove central FullRuntimeSceneResetHandler

```text
DELETE crates/runtime/bundles/src/scene_reset.rs
MODIFY crates/runtime/bundles/src/lib.rs
MODIFY crates/apps/app/src/bootstrap.rs
VALIDATE rg FullRuntimeSceneResetHandler
```

## Commit 7 — component metadata dynamic key/provider

```text
MODIFY component_metadata.rs
MODIFY component_metadata_provider.rs
MODIFY scene/plugin.rs
VALIDATE cargo check -p amigo-scene
```

## Commit 8 — migrate first component metadata providers

```text
ADD sprite metadata provider
ADD layered-image metadata provider
ADD text metadata provider
ADD vector metadata provider
MODIFY default_component_registry remove migrated entries
VALIDATE targeted
```

## Commit 9 — devtools cleanup

```text
DELETE stale command files
SPLIT completion.rs
EXTEND completion provider API
MOVE postfx kinds into composite provider
VALIDATE cargo check -p amigo-devtools
```

## Commit 10 — split render extractor bridges

```text
MOVE world_2d.rs into folder modules
MOVE light_sources_2d.rs into folder modules
VALIDATE cargo check -p amigo-runtime-bundles
```

## Commit 11 — move app render helpers out

```text
MOVE render_runtime/services.rs to runtime-bundles
MOVE render_runtime/graph.rs to render-api
MODIFY app imports
VALIDATE cargo check -p amigo-app
```

## Commit 12 — render-wgpu adapter preparation

```text
MODIFY frame_packet.rs add Renderable2dPayloadKind
ADD renderable_adapter.rs
MODIFY render-wgpu/lib.rs
VALIDATE cargo check -p amigo-render-wgpu
```

## Commit 13 — camera debug view cleanup

```text
MODIFY engine/camera/debug_view.rs
MODIFY render-api/camera_capture.rs
ADD relight debug descriptors
VALIDATE cargo check -p amigo-camera -p amigo-render-api
```

## Commit 14 — behavior relocation

```text
MOVE crates/engine/behavior -> plugins/gameplay/behavior
MODIFY Cargo.toml
MODIFY docs
VALIDATE cargo metadata --no-deps
```

---

# Oczekiwany efekt

Po commitach 1–6:

```text
runtime reset przestaje być centralny
apps/app jeszcze prostsze
runtime/bundles mniej monolityczne
```

Po commitach 7–10:

```text
component metadata i devtools przestają być centralnymi katalogami
render extractor bridge przestaje być 1000-liniowym hubem
```

Po commitach 11–14:

```text
app przestaje być runtime coordinator
render-wgpu przygotowane pod adaptery
camera debug views mniej plugin-specific
behavior nie udaje engine core
```

Docelowa ocena po tym planie:

```text
Ogólnie: 8.9–9.1/10
Camera-first identity: 9.1/10
Prostota architektoniczna: 7.4–7.8/10
Spójność warstw: 8.9–9.2/10
Pluginowość: 9.1/10
```

Najważniejszy skrót dla agenta:

```text
Nie usuwać złożoności przez kasowanie funkcji.
Usuwać złożoność przez przenoszenie odpowiedzialności do właścicieli:
reset do pluginów,
metadata do pluginów,
commands/completions do pluginów,
render extraction do małych bridge modules,
frame graph do render-api,
host orchestration z app do session/bundles.
```
