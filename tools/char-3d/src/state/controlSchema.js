export const rangeControls = {
    yaw: ['yaw', v => `${Math.round(v)}°`],
    pitch: ['pitch', v => `${Math.round(v)}°`],
    zoom: ['zoom', v => Number(v).toFixed(2)],
    focalLength: ['focalLength', v => `${Math.round(v)}mm`],
    cameraYaw: ['cameraYaw', v => `${Math.round(v)}°`],
    cameraPitch: ['cameraPitch', v => `${Math.round(v)}°`],
    cameraX: ['cameraX', v => Number(v).toFixed(2)],
    cameraY: ['cameraY', v => Number(v).toFixed(2)],
    cameraZ: ['cameraZ', v => Number(v).toFixed(2)],
    tweenJitterFrames: ['tweenJitterFrames', v => `${Number(v).toFixed(2)}f`],
    lightAz: ['lightAz', v => `${Math.round(v)}°`],
    lightEl: ['lightEl', v => `${Math.round(v)}°`],
    density: ['density', v => Number(v).toFixed(2)],
    layers: ['layers', v => String(Math.round(v))],
    threshold: ['threshold', v => Number(v).toFixed(2)],
    core: ['core', v => Number(v).toFixed(2)],
    contact: ['contact', v => Number(v).toFixed(2)],
    edgeDark: ['edgeDark', v => Number(v).toFixed(2)],
    simplify: ['simplify', v => Number(v).toFixed(2)],
    economy: ['economy', v => Number(v).toFixed(2)],
    cleanupMinFaceAreaPx: ['cleanupMinFaceAreaPx', v => `${Number(v).toFixed(1)}px`],
    cleanupMinLineLengthPx: ['cleanupMinLineLengthPx', v => `${Number(v).toFixed(1)}px`],
    cleanupMaxEdgeLengthPx: ['cleanupMaxEdgeLengthPx', v => `${Math.round(v)}px`],
    cleanupDensityClamp: ['cleanupDensityClamp', v => Number(v).toFixed(2)],
    cleanupRegionMinAreaPx: ['cleanupRegionMinAreaPx', v => `${Math.round(v)}px²`],
    cleanupRegionMinFaces: ['cleanupRegionMinFaces', v => `${Math.round(v)}`],
    cleanupRegionMaxAspect: ['cleanupRegionMaxAspect', v => Number(v).toFixed(1)],
    hairRegionSuppression: ['hairRegionSuppression', v => Number(v).toFixed(2)],
    scenePartitionCellSize: ['scenePartitionCellSize', v => `${Math.round(v)}`],
    scenePartitionMaxUnits: ['scenePartitionMaxUnits', v => `${Math.round(v)}`],
    visibilityMarginPx: ['visibilityMarginPx', v => `${Math.round(v)}px`],
    visibilityMinAreaPx: ['visibilityMinAreaPx', v => `${Number(v).toFixed(1)}px²`],
    visibilityMinRadiusPx: ['visibilityMinRadiusPx', v => `${Number(v).toFixed(1)}px`],
    detailTier0RadiusPx: ['detailTier0RadiusPx', v => `${Math.round(v)}px`],
    detailTier1RadiusPx: ['detailTier1RadiusPx', v => `${Math.round(v)}px`],
    detailTier2RadiusPx: ['detailTier2RadiusPx', v => `${Math.round(v)}px`],
    detailTier3RadiusPx: ['detailTier3RadiusPx', v => `${Math.round(v)}px`],
    detailDensityPenalty: ['detailDensityPenalty', v => Number(v).toFixed(2)],
    detailImportanceBias: ['detailImportanceBias', v => Number(v).toFixed(2)],
    vectorMaxProjectedFaces: ['vectorMaxProjectedFaces', v => `${Math.round(v)}`],
    vectorMaxVisibleEdges: ['vectorMaxVisibleEdges', v => `${Math.round(v)}`],
    vectorMaxContourLines: ['vectorMaxContourLines', v => `${Math.round(v)}`],
    vectorMaxShadowMarks: ['vectorMaxShadowMarks', v => `${Math.round(v)}`],
    vectorMinFaceAreaPx: ['vectorMinFaceAreaPx', v => `${Number(v).toFixed(1)}px²`],
    vectorMinEdgeLengthPx: ['vectorMinEdgeLengthPx', v => `${Number(v).toFixed(1)}px`],
    regionMinProjectedAreaPx: ['regionMinProjectedAreaPx', v => `${Number(v).toFixed(1)}px²`],
    regionMaxPaintRegions: ['regionMaxPaintRegions', v => `${Math.round(v)}`],
    shadowBandCount: ['shadowBandCount', v => `${Math.round(v)}`],
    shadowRegionBleed: ['shadowRegionBleed', v => Number(v).toFixed(2)],
    shadowColorJitter: ['shadowColorJitter', v => Number(v).toFixed(2)],
    strokePressureJitter: ['strokePressureJitter', v => Number(v).toFixed(2)],
    temporalCoherence: ['temporalCoherence', v => Number(v).toFixed(2)],
    projectionHumanError: ['projectionHumanError', v => Number(v).toFixed(2)],
    strokeLen: ['strokeLen', v => `${Math.round(v)}`],
    spacing: ['spacing', v => `${Math.round(v)}`],
    strokeWidth: ['strokeWidth', v => Number(v).toFixed(2)],
    curvature: ['curvature', v => Number(v).toFixed(2)],
    crossAngle: ['crossAngle', v => `${Math.round(v)}°`],
    dotSize: ['dotSize', v => Number(v).toFixed(1)],
    wobble: ['wobble', v => Number(v).toFixed(2)],
    jitter: ['jitter', v => Number(v).toFixed(2)],
    strokeCrookedness: ['strokeCrookedness', v => Number(v).toFixed(2)],
    strokeKinkChance: ['strokeKinkChance', v => Number(v).toFixed(2)],
    strokeToneRamp: ['strokeToneRamp', v => Number(v).toFixed(2)],
    shadowFrameDrift: ['shadowFrameDrift', v => Number(v).toFixed(2)],
    shadowLoopRedraw: ['shadowLoopRedraw', v => Number(v).toFixed(2)],
    shadowLayoutJitter: ['shadowLayoutJitter', v => Number(v).toFixed(2)],
    projectionWobble: ['projectionWobble', v => Number(v).toFixed(2)],
    spacingVar: ['spacingVar', v => Number(v).toFixed(2)],
    lengthVar: ['lengthVar', v => Number(v).toFixed(2)],
    widthVar: ['widthVar', v => Number(v).toFixed(2)],
    taper: ['taper', v => Number(v).toFixed(2)],
    breakup: ['breakup', v => Number(v).toFixed(2)],
    overdraw: ['overdraw', v => Number(v).toFixed(2)],
    contourDrift: ['contourDrift', v => Number(v).toFixed(2)],
    contourWobble: ['contourWobble', v => Number(v).toFixed(2)],
    contourGaps: ['contourGaps', v => Number(v).toFixed(2)],
    contourFrameVariance: ['contourFrameVariance', v => Number(v).toFixed(2)],
    paintBaseOpacity: ['paintBaseOpacity', v => Number(v).toFixed(2)],
    paintWashOpacity: ['paintWashOpacity', v => Number(v).toFixed(2)],
    paintCelStrength: ['paintCelStrength', v => Number(v).toFixed(2)],
    paintCelSteps: ['paintCelSteps', v => String(Math.round(v))],
    paintHighlightAmount: ['paintHighlightAmount', v => Number(v).toFixed(2)],
    paintHalftone: ['paintHalftone', v => Number(v).toFixed(2)],
    paintHalftoneScale: ['paintHalftoneScale', v => `${Math.round(v)}`],
    paintRegistration: ['paintRegistration', v => Number(v).toFixed(2)],
    paintBleed: ['paintBleed', v => Number(v).toFixed(2)],
    paintGrain: ['paintGrain', v => Number(v).toFixed(2)],
    paintRegionResolution: ['paintRegionResolution', v => `${Math.round(v)}px`],
    paintRegionSimplify: ['paintRegionSimplify', v => Number(v).toFixed(2)],
    paintEdgeBleed: ['paintEdgeBleed', v => Number(v).toFixed(2)],
    paintPigmentGranulation: ['paintPigmentGranulation', v => Number(v).toFixed(2)],
    paintRegionJitter: ['paintRegionJitter', v => Number(v).toFixed(2)],
    paintWetMix: ['paintWetMix', v => Number(v).toFixed(2)],
    inkDominance: ['inkDominance', v => Number(v).toFixed(2)],
    depthEps: ['depthEps', v => Number(v).toFixed(3)]
  };

export const colorControls = ['paintPaperColor','paintBaseColor','paintShadowColor','paintHighlightColor'];

export const checkControls = ['auto','faceWash','paintEnabled','contours','shadowsEnabled','impreciseTween','contourHumanize','hideOccluded','backface','depthClipStrokes','clipToFaces','showHidden','sortFaces','creases','suggestive','contactLines','depthFade','scenePartitionEnabled','visibilityCullingEnabled','detailPolicyEnabled','vectorBudgetEnabled','regionBudgetEnabled','regionAllowFarFills','mainContourEnabled','creaseAccentEnabled','suggestiveContourEnabled','hiddenLineEnabled','shadowHatchEnabled','baseWashEnabled','shadowRegionEnabled','highlightRegionEnabled','tone','flow','depthDebug','seedDebug','regionDebug','cleanupDebug','densityDebug','visibilityDebug','detailDebug','budgetDebug','skipSimulation'];

