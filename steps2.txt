Poniżej pełny backlog tego, co zostało po snapshotcie `concat-output-bq7af`, w tym samym stylu operacyjnym: `ADD / MODIFY / MOVE / DELETE / VERIFY`. To jest plan od obecnego stanu, nie od zera.

# METAPROMPT DLA AGENTA

```txt
Tryb pracy:
- Pracuj jako zdalny LLM-agent wykonujący operacje na repo.
- Każdy krok ma typ: ADD / MODIFY / MOVE / DELETE / VERIFY.
- Nie rób legacy bridge.
- Nie twórz deprecated wrapperów.
- Nie używaj nazw *_v2.
- Nie utrzymuj starego API dla kompatybilności.
- Jeżeli coś przenosisz do pluginu, usuń starą lokalizację.
- Jeżeli stara lokalizacja nadal istnieje po MOVE, to krok nie jest skończony.
- Build może być zepsuty w środku fali.
- Milestone musi kończyć się walidacją.
```

# R0 — stan obecny

```txt
Zrobione:
- plugin-api
- codemap-api
- plugin-index
- plugin-manifest
- plugin-loader
- plugins/ root
- 27 plugin.toml
- dużo scaffoldów pluginów
- częściowy camera/gfx/lighting/materials/vfx/postfx split

Blokery:
- Cargo.toml wskazuje 6 missing workspace members.
- część pluginów nie ma pełnego canonical tree.
- devtools/codemap ma plugin.toml, ale brak Cargo.toml.
- istnieją równolegle stare crate’y domenowe.
- scene/runtime/render/scripting nadal mają dużo domenowej logiki.
```

---

# KROK 41 — naprawić workspace blockers

## OPERATION: ADD

Dodać brakujące workspace members:

```txt
crates/tools/scene-preview/
crates/tools/scene-snapshot/
crates/tools/amigo-codemap/
crates/tools/amigo-symbol-explorer/
crates/tools/plugin-check/
crates/tools/plugin-new/
```

Minimalna struktura każdego tool crate:

```txt
Cargo.toml
src/main.rs
```

## VERIFY

```bash
python - <<'PY'
import tomllib, pathlib
root = pathlib.Path(".")
members = tomllib.load(open("Cargo.toml","rb"))["workspace"]["members"]
missing = [m for m in members if not (root / m).exists()]
print(missing)
assert not missing
PY
```

## DO NOT

```txt
- Nie usuwaj tych members z Cargo.toml.
- Nie twórz pustych folderów bez Cargo.toml.
- Nie podpinaj tools do runtime.
```

## ACCEPTANCE

```txt
workspace members mają fizyczne katalogi.
```

---

# KROK 42 — dokończyć `plugin-check`

## OPERATION: ADD / MODIFY

Target:

```txt
crates/tools/plugin-check/
```

Tool ma sprawdzać:

```txt
1. Każdy plugin ma plugin.toml.
2. Każdy plugin ma Cargo.toml.
3. Każdy plugin ma canonical tree.
4. plugin.toml family zgadza się z plugins/<family>/.
5. plugin.toml id jest unikalne.
6. docs/pipeline.md istnieje.
7. tests/waterfall_tests.rs istnieje.
8. brak src/render-wgpu po normalizacji.
9. brak legacy/deprecated/_v2.
10. brak renderer-side guessing.
```

## VERIFY

```bash
cargo run -p plugin-check -- plugins
```

## DO NOT

```txt
- Nie parsuj runtime state.
- Nie skanuj crates/ jako źródła pluginów.
- Nie dopuszczaj soft warnings dla canonical tree.
```

---

# KROK 43 — dokończyć `plugin-new`

## OPERATION: ADD / MODIFY

Target:

```txt
crates/tools/plugin-new/
templates/plugin/
```

Komenda:

```bash
cargo run -p plugin-new -- --family gfx --name example-2d --kind renderable-source
```

Musi generować:

```txt
plugins/<family>/<name>/
  Cargo.toml
  plugin.toml
  README.md
  src/...
  tests/...
  docs/...
```

## VERIFY

```bash
cargo run -p plugin-new -- --family devtools --name test-plugin --kind tooling
cargo run -p plugin-check -- plugins/devtools/test-plugin
```

## DO NOT

```txt
- Nie generuj render-wgpu.
- Generuj render_wgpu.
- Nie dodawaj pluginu automatycznie do runtime.
```

---

# KROK 44 — poprawić canonical tree: `render-wgpu` → `render_wgpu`

Obecny problem: w pluginach są foldery `src/render-wgpu` i `src/render_wgpu`. Rust module path nie może używać `render-wgpu`.

## OPERATION: MOVE / MODIFY / DELETE

Dla każdego pluginu:

```txt
MOVE:
src/render-wgpu/* -> src/render_wgpu/*

DELETE:
src/render-wgpu/
```

Zmienić dokumentację:

```txt
docs/architecture/plugins/canonical-tree.md
templates/plugin/
```

Wszędzie standard ma być:

```txt
src/render_wgpu/
```

Nie:

```txt
src/render-wgpu/
```

## VERIFY

```bash
find plugins templates -path '*render-wgpu*'
```

Expected:

```txt
brak wyników
```

## DO NOT

```txt
- Nie zostawiaj dwóch folderów.
- Nie mapuj przez #[path = "..."].
```

---

# KROK 45 — uzupełnić canonical tree we wszystkich pluginach

## OPERATION: ADD

Dla każdego pluginu z `plugins/*/*/plugin.toml` uzupełnić brakujące pliki z template.

Najbardziej niepełne obecnie:

```txt
plugins/devtools/codemap
plugins/postfx/scopes
plugins/postfx/composite
plugins/postfx/debug-views
plugins/postfx/color-grading
plugins/lighting/lightmaps-2d
plugins/lighting/relight-2d
plugins/materials/material-maps
plugins/materials/procedural-materials
plugins/vfx/trails-2d
```

## VERIFY

```bash
cargo run -p plugin-check -- plugins
```

## DO NOT

```txt
- Nie kopiuj logiki domenowej na ślepo.
- Brak logiki = noop.rs, ale plik musi istnieć.
- Nie zostawiaj brakujących README.md.
```

---

# KROK 46 — dodać `Cargo.toml` dla `plugins/devtools/codemap`

## OPERATION: ADD

Target:

```txt
plugins/devtools/codemap/Cargo.toml
```

Package name:

```txt
amigo-codemap-plugin
```

Dodać do root `Cargo.toml`:

```toml
"plugins/devtools/codemap",
```

Dodać workspace dependency:

```toml
amigo-codemap-plugin = { path = "plugins/devtools/codemap" }
```

## VERIFY

```bash
cargo check -p amigo-codemap-plugin
```

## DO NOT

```txt
- Nie zostawiaj plugin.toml bez Cargo.toml.
- Nie podpinaj jeszcze do app/devtools.
```

---

# KROK 47 — dodać environment/editor jako puste rodziny

## OPERATION: ADD

Jeżeli nie migrujemy jeszcze environment/editor, zostawić tylko:

```txt
plugins/environment/.gitkeep
plugins/editor/.gitkeep
```

## VERIFY

```bash
find plugins/environment plugins/editor -maxdepth 2 -type f
```

## DO NOT

```txt
- Nie dodawaj atmosphere.
- Nie dodawaj rain/fog/smoke.
- Nie migruj editor tools teraz.
```

---

# KROK 48 — stworzyć inventory migracji

## OPERATION: ADD

File:

```txt
docs/architecture/plugins/migration-inventory.md
```

Tabela:

```txt
current_path | target_plugin | target_path | operation | status
```

Wypełnić dla:

```txt
crates/engine/camera
crates/engine/scene
crates/runtime/bundles
crates/engine/render-wgpu
crates/scripting/rhai
crates/2d/sprite
crates/2d/text
crates/2d/vector
crates/2d/tilemap
crates/2d/layered-image
crates/2d/lighting
crates/2d/particles
crates/2d/post-fx
crates/2d/motion
crates/engine/render-api/src/material2d.rs
crates/engine/render-api/src/light_source_2d.rs
```

## VERIFY

```bash
rg "TODO|unmapped|unknown" docs/architecture/plugins/migration-inventory.md
```

Expected:

```txt
brak wyników przed rozpoczęciem MOVE
```

---

# KROK 49 — domknąć camera family MOVE

## OPERATION: MOVE

Z `crates/engine/camera/src/` przenieść:

```txt
model.rs            -> plugins/camera/camera-core/src/api/model.rs
projection.rs       -> plugins/camera/camera-core/src/api/projection.rs
viewport.rs         -> plugins/camera/camera-core/src/api/viewport.rs
screen_world.rs     -> plugins/camera/camera-core/src/runtime/screen_world.rs
control.rs          -> plugins/camera/camera-core/src/runtime/control.rs
rig.rs              -> plugins/camera/camera-core/src/runtime/rig.rs
service.rs          -> plugins/camera/camera-core/src/runtime/service.rs
systems.rs          -> plugins/camera/camera-core/src/runtime/systems.rs
runtime_capabilities.rs -> plugins/camera/camera-core/src/api/capabilities.rs

focus_targets.rs    -> plugins/camera/focus-depth/src/runtime/focus_targets.rs
optics.rs           -> plugins/camera/camera-optics/src/runtime/optics.rs
film_grain.rs       -> plugins/camera/film-look/src/runtime/film_grain.rs
profiles.rs         -> plugins/camera/camera-profiles/src/runtime/profiles.rs
quality.rs          -> plugins/camera/camera-profiles/src/api/quality.rs
scene_command.rs    -> odpowiednie plugins/camera/*/src/scene/commands.rs
editor_capability.rs -> plugins/camera/camera-core/src/diagnostics/editor_capability.rs
```

## OPERATION: MODIFY

Zaktualizować importy:

```txt
amigo_camera::... -> amigo_camera_core_plugin::...
amigo_camera::profiles::... -> amigo_camera_profiles_plugin::...
amigo_camera::optics::... -> amigo_camera_optics_plugin::...
```

## OPERATION: DELETE

Po pełnym przeniesieniu:

```txt
crates/engine/camera/
```

Usunąć z workspace members i workspace dependencies:

```txt
amigo-camera
```

## VERIFY

```bash
rg "amigo_camera|crates/engine/camera|amigo-camera" crates plugins Cargo.toml
cargo check --workspace
```

## DO NOT

```txt
- Nie zostawiaj crates/engine/camera jako wrappera.
- Nie rób re-exportów.
- Nie zostawiaj `amigo-camera` w workspace dependencies.
```

---

# KROK 50 — domknąć camera-optics semantics

## OPERATION: MODIFY

Target:

```txt
plugins/camera/camera-optics/
```

Wymusić reguły:

```txt
1. Brak contribution = brak candidate.
2. Unsupported coverage = no active candidate.
3. Renderer nie generuje highlight z luma fallback.
4. Target/gain logic żyje w candidate/targets.
5. LightMapChannel jest konsumowany przez plugin render_wgpu.
```

## OPERATION: DELETE

Usunąć z innych miejsc:

```txt
should_produce_scene_highlight
luma_fallback
direct_lens_flare
guess_optical
```

## VERIFY

```bash
rg "should_produce_scene_highlight|luma_fallback|direct_lens_flare|guess_optical" crates plugins
cargo test -p amigo-camera-optics-plugin
```

---

# KROK 51 — usunąć stare lens/flare fields z beacon/light scene

## OPERATION: MODIFY

Targety:

```txt
crates/engine/scene/src/document/components.rs
crates/engine/scene/src/render_commands/render_2d.rs
crates/engine/scene/src/document/defaults.rs
plugins/lighting/beacon-light-2d/
plugins/lighting/light-2d/
mods/rotten-club/
```

Zastąpić pola:

```txt
flare_strength
flare_length_px
lens_influence
lens_bloom
flare_ghosts
```

przez jawne:

```txt
contributions.camera_optics
```

## VERIFY

```bash
rg "flare_strength|flare_length_px|lens_influence|lens_bloom|flare_ghosts" crates plugins mods
```

Expected:

```txt
brak wyników albo tylko testy migracyjne, które też potem usunąć
```

## DO NOT

```txt
- Light/Beacon nie może rysować lens flare.
- Light/Beacon może tylko emitować camera_optics contribution.
```

---

# KROK 52 — domknąć `focus-depth`

## OPERATION: MOVE / MODIFY

Przenieść logikę depth/focus z:

```txt
crates/2d/depth-map
crates/engine/camera/src/focus_targets.rs
crates/runtime/bundles/src/focus_targets_2d.rs
```

do:

```txt
plugins/camera/focus-depth/
```

## VERIFY

```bash
rg "focus_targets|depth_map|DepthMap|FocusDepth" crates plugins
cargo test -p amigo-focus-depth-plugin
```

## DO NOT

```txt
- Nie zostawiaj focus/depth logiki w runtime/bundles.
- Nie mieszaj focus-depth z camera-optics.
```

---

# KROK 53 — domknąć `shutter-motion`

## OPERATION: MOVE / MODIFY

Przenieść motion/temporal/shutter logic z:

```txt
crates/2d/motion
crates/2d/particles
crates/engine/render-wgpu/src/post_fx
```

do:

```txt
plugins/camera/shutter-motion/
plugins/vfx/particles-2d/
plugins/vfx/trails-2d/
```

## VERIFY

```bash
rg "shutter|motion_blur|temporal|velocity|trail" crates plugins
cargo test -p amigo-shutter-motion-plugin
```

## DO NOT

```txt
- Particles nie wykonują shutter effect.
- Particles emitują motion/shutter contribution.
```

---

# KROK 54 — domknąć `film-look` i `camera-profiles`

## OPERATION: MOVE / MODIFY

Przenieść:

```txt
film grain
sensor response
tone curve
camera quality
camera profiles
```

do:

```txt
plugins/camera/film-look/
plugins/camera/camera-profiles/
```

## VERIFY

```bash
rg "film_grain|tone_curve|camera_profile|quality" crates plugins
cargo test -p amigo-film-look-plugin
cargo test -p amigo-camera-profiles-plugin
```

## DO NOT

```txt
- Film-look nie może siedzieć w postfx jako przypadkowy pass.
- Profiles nie mogą zostać w engine/camera.
```

---

# KROK 55 — przenieść GFX: sprite/text/vector

## OPERATION: MOVE

```txt
crates/2d/sprite -> plugins/gfx/sprite-2d
crates/2d/text   -> plugins/gfx/text-2d
crates/2d/vector -> plugins/gfx/vector-2d
```

## OPERATION: MODIFY

Aktualizować importy:

```txt
amigo_2d_sprite -> amigo_sprite_2d_plugin
amigo_2d_text   -> amigo_text_2d_plugin
amigo_2d_vector -> amigo_vector_2d_plugin
```

## OPERATION: DELETE

```txt
crates/2d/sprite
crates/2d/text
crates/2d/vector
```

Usunąć stare crate’y z workspace.

## VERIFY

```bash
rg "amigo_2d_sprite|amigo_2d_text|amigo_2d_vector" crates plugins Cargo.toml
cargo test -p amigo-sprite-2d-plugin
cargo test -p amigo-text-2d-plugin
cargo test -p amigo-vector-2d-plugin
```

---

# KROK 56 — przenieść GFX: layered-image/tilemap

## OPERATION: MOVE

```txt
crates/2d/layered-image -> plugins/gfx/layered-image-2d
crates/2d/tilemap       -> plugins/gfx/tilemap-2d
```

## DELETE

```txt
crates/2d/layered-image
crates/2d/tilemap
```

## VERIFY

```bash
rg "amigo_2d_layered|amigo_2d_tilemap|layered_image|tilemap" crates plugins Cargo.toml
cargo test -p amigo-layered-image-2d-plugin
cargo test -p amigo-tilemap-2d-plugin
```

---

# KROK 57 — przenieść lighting family

## OPERATION: MOVE

```txt
crates/2d/lighting/src/light*      -> plugins/lighting/light-2d
crates/2d/lighting/src/groups*     -> plugins/lighting/light-groups-2d
crates/2d/lighting/src/lightmap*   -> plugins/lighting/lightmaps-2d
crates/2d/lighting/beacon/*        -> plugins/lighting/beacon-light-2d
relight-related code               -> plugins/lighting/relight-2d
```

## DELETE

```txt
crates/2d/lighting
```

## VERIFY

```bash
rg "amigo_2d_lighting|BeaconLight|LightGroup|LightMap|Relight" crates plugins Cargo.toml
cargo test -p amigo-light-2d-plugin
cargo test -p amigo-light-groups-2d-plugin
cargo test -p amigo-lightmaps-2d-plugin
cargo test -p amigo-beacon-light-2d-plugin
cargo test -p amigo-relight-2d-plugin
```

## DO NOT

```txt
- Beacon nie może mieć direct lens flare.
- Light nie może znać final camera-optics effect.
```

---

# KROK 58 — przenieść materials family

## OPERATION: MOVE

Przenieść z:

```txt
crates/engine/render-api/src/material2d.rs
crates/engine/scene/src/document/material2d.rs
crates/engine/scene/src/hydration/material*
crates/engine/render-wgpu/src/material*
```

do:

```txt
plugins/materials/material-2d/
plugins/materials/material-maps/
plugins/materials/procedural-materials/
```

## VERIFY

```bash
rg "Material2d|material2d|procedural_material|material_map" crates plugins
cargo test -p amigo-material-2d-plugin
cargo test -p amigo-material-maps-plugin
cargo test -p amigo-procedural-materials-plugin
```

## DO NOT

```txt
- render-api nie powinien trzymać domenowej logiki materiału.
- material nie wykonuje optics/relight; tylko emituje contributions.
```

---

# KROK 59 — przenieść VFX family

## OPERATION: MOVE

```txt
crates/2d/particles -> plugins/vfx/particles-2d
trail-related code   -> plugins/vfx/trails-2d
```

## VERIFY

```bash
rg "amigo_2d_particles|Particle|Trail" crates plugins Cargo.toml
cargo test -p amigo-particles-2d-plugin
cargo test -p amigo-trails-2d-plugin
```

## DO NOT

```txt
- Particles nie robią shutter/focus/camera-optics bezpośrednio.
- Particles emitują contributions.
```

---

# KROK 60 — przenieść postfx family

## OPERATION: MOVE

```txt
crates/2d/post-fx
crates/engine/render-wgpu/src/post_fx
```

do:

```txt
plugins/postfx/bloom/
plugins/postfx/color-grading/
plugins/postfx/scopes/
plugins/postfx/composite/
plugins/postfx/debug-views/
```

## DELETE

```txt
crates/2d/post-fx
```

## VERIFY

```bash
rg "amigo_2d_post_fx|PostFx|Bloom|ColorGrading|Scopes|Composite" crates plugins Cargo.toml
cargo test -p amigo-bloom-plugin
cargo test -p amigo-color-grading-plugin
cargo test -p amigo-scopes-plugin
cargo test -p amigo-composite-plugin
cargo test -p amigo-debug-views-plugin
```

## DO NOT

```txt
- camera-optics nie może być ukryte w postfx.
- postfx nie zgaduje targetów.
```

---

# KROK 61 — oczyścić `engine/scene`

## OPERATION: MOVE

Z `crates/engine/scene` przenieść plugin-owned:

```txt
document/camera.rs              -> plugins/camera/*/src/scene/document.rs
document/material2d.rs          -> plugins/materials/material-2d/src/scene/document.rs
document/visual2d/lighting.rs   -> plugins/lighting/*/src/scene/document.rs
document/visual2d/post_fx*.rs   -> plugins/postfx/*/src/scene/document.rs
component_metadata.rs           -> plugin-owned descriptors
render_commands/render_2d.rs    -> plugin-owned scene commands
hydration/post_fx.rs            -> plugins/postfx/*/src/scene/hydration.rs
```

## OPERATION: MODIFY

`crates/engine/scene` zostaje tylko jako:

```txt
SceneDocument envelope
ComponentRegistry
CommandEnvelope
HydrationRegistry
Validation framework
```

## VERIFY

```bash
rg "CameraLens|Material2d|LightGroup|Beacon|PostFx|Sprite|Text2d|Vector" crates/engine/scene/src
```

Expected:

```txt
brak domenowej logiki; dopuszczalne tylko generic registry names
```

## DO NOT

```txt
- Scene nie może być globalnym workiem komponentów.
- Nie zostawiaj component_metadata jako monolitu.
```

---

# KROK 62 — oczyścić `runtime/bundles`

## OPERATION: MOVE

Przenieść:

```txt
crates/runtime/bundles/src/render_extractor_bridges/*
crates/runtime/bundles/src/focus_targets_2d.rs
```

do odpowiednich:

```txt
plugins/*/*/src/runtime/extract.rs
plugins/*/*/src/runtime/collect.rs
```

## OPERATION: MODIFY

`runtime/bundles` ma zostać jako composition layer:

```txt
plugin_composition.rs
core.rs
full.rs
two_d.rs
three_d.rs
```

bez domenowej ekstrakcji.

## VERIFY

```bash
rg "render_extractor_bridges|focus_targets_2d|LightSource|Sprite|CameraOptical|PostFx" crates/runtime/bundles/src
```

## DO NOT

```txt
- runtime/bundles nie może znać internali pluginów.
- bundle składa pluginy, nie implementuje domen.
```

---

# KROK 63 — oczyścić `render-wgpu`

## OPERATION: MOVE

Przenieść domain render logic z:

```txt
crates/engine/render-wgpu/src/
```

do:

```txt
plugins/*/*/src/render_wgpu/
```

`render-wgpu` zostaje jako:

```txt
backend
device/context
target allocator
render graph executor
pipeline cache
buffer allocator
frame submit
```

## VERIFY

```bash
rg "CameraOptical|FocusDepth|ShutterMotion|FilmLook|Sprite|Text2d|Vector|LightGroup|Beacon|Material2d|PostFx|Bloom" crates/engine/render-wgpu/src
```

Expected:

```txt
brak domenowej semantyki; tylko generic backend hooks
```

## DO NOT

```txt
- Renderer nie zgaduje domen.
- Renderer nie tworzy targetów z luma fallback.
- Renderer nie zna lens flare.
```

---

# KROK 64 — oczyścić `scripting-rhai`

## OPERATION: MOVE

Przenieść bindingi domenowe z:

```txt
crates/scripting/rhai/src/
```

do:

```txt
plugins/*/*/src/scripting/
```

## OPERATION: MODIFY

`scripting-rhai` zostaje jako:

```txt
RhaiHost
BindingRegistry
ScriptRuntime
```

## VERIFY

```bash
rg "camera|sprite|text|vector|light|beacon|material|post_fx|lens|flare" crates/scripting/rhai/src
```

Expected:

```txt
brak domenowych setterów/getterów
```

## DO NOT

```txt
- Rhai nie może omijać scene validation.
- Globalny scripting nie zna domen.
```

---

# KROK 65 — oczyścić app/devtools

## OPERATION: MOVE / MODIFY

Domenowe diagnostics przenieść do:

```txt
plugins/*/*/src/diagnostics/
```

App/devtools zostają jako agregatory:

```txt
diagnostics viewer
plugin browser
pipeline inspector
scene inspector
```

## VERIFY

```bash
rg "CameraOptical|FocusDepth|Sprite|LightGroup|Beacon|Material2d|PostFx|Bloom" crates/apps crates/engine/devtools
```

Expected:

```txt
app/devtools nie zawiera domenowej logiki, tylko registry/channel aggregation
```

---

# KROK 66 — dokończyć codemap plugin

## OPERATION: ADD / MODIFY

Target:

```txt
plugins/devtools/codemap/
crates/tools/amigo-codemap/
```

Codemap ma czytać plugin loader output i pokazywać:

```txt
plugin graph
slot graph
capability graph
target graph
contribution graph
diagnostics graph
missing canonical files
orphan targets
unused contributions
```

## VERIFY

```bash
cargo run -p amigo-codemap -- plugins
```

---

# KROK 67 — dokończyć symbol explorer

## OPERATION: ADD / MODIFY

Target:

```txt
crates/tools/amigo-symbol-explorer/
```

Na razie wystarczy:

```txt
- indeksuje plugin folders
- pokazuje symbole per plugin
- pokazuje file ownership
- nie parsuje całego Rusta semantycznie
```

## VERIFY

```bash
cargo run -p amigo-symbol-explorer -- plugins/camera/camera-optics
```

---

# KROK 68 — przepisać Rotten Club content

## OPERATION: MODIFY

Targety:

```txt
mods/rotten-club/
```

Zamienić stare pola:

```txt
flare_strength
lens_bloom
flare_ghosts
direct lens config in light/beacon
```

na:

```txt
contributions:
  camera_optics:
    enabled: true
    response: ...
    coverage: ...
```

## VERIFY

```bash
rg "flare_strength|lens_bloom|flare_ghosts|lens_influence" mods/rotten-club
cargo run -p plugin-check -- plugins
```

## DO NOT

```txt
- Nie zostawiaj contentu zależnego od starej semantyki.
```

---

# KROK 69 — globalne DELETE starych crate’ów domenowych

## OPERATION: DELETE

Po pełnym MOVE usunąć:

```txt
crates/engine/camera
crates/2d/sprite
crates/2d/text
crates/2d/vector
crates/2d/tilemap
crates/2d/layered-image
crates/2d/lighting
crates/2d/particles
crates/2d/post-fx
crates/2d/motion
```

Jeżeli `crates/2d/depth-map` zostało w całości w `focus-depth`, usunąć też:

```txt
crates/2d/depth-map
```

## OPERATION: MODIFY

Usunąć z root `Cargo.toml`:

```txt
workspace members
workspace dependencies
references in package deps
```

## VERIFY

```bash
rg "amigo-2d-sprite|amigo-2d-text|amigo-2d-vector|amigo-2d-lighting|amigo-2d-post-fx|amigo-camera" Cargo.toml crates plugins
```

Expected:

```txt
brak wyników
```

---

# KROK 70 — globalne zakazy architektoniczne

## OPERATION: VERIFY

Dodać do `plugin-check` twarde reguły:

```txt
forbidden:
- legacy
- deprecated
- _v2
- re-export
- reexport
- luma_fallback
- should_produce_scene_highlight
- direct_lens_flare
- guess_optical
- flare_strength
- lens_influence
```

## VERIFY

```bash
cargo run -p plugin-check -- plugins crates mods
```

---

# KROK 71 — finalne testy per plugin family

## OPERATION: VERIFY

```bash
cargo test -p amigo-camera-core-plugin
cargo test -p amigo-camera-optics-plugin
cargo test -p amigo-focus-depth-plugin
cargo test -p amigo-shutter-motion-plugin
cargo test -p amigo-film-look-plugin
cargo test -p amigo-camera-profiles-plugin

cargo test -p amigo-sprite-2d-plugin
cargo test -p amigo-text-2d-plugin
cargo test -p amigo-vector-2d-plugin
cargo test -p amigo-layered-image-2d-plugin
cargo test -p amigo-tilemap-2d-plugin

cargo test -p amigo-light-2d-plugin
cargo test -p amigo-light-groups-2d-plugin
cargo test -p amigo-lightmaps-2d-plugin
cargo test -p amigo-beacon-light-2d-plugin
cargo test -p amigo-relight-2d-plugin

cargo test -p amigo-material-2d-plugin
cargo test -p amigo-material-maps-plugin
cargo test -p amigo-procedural-materials-plugin

cargo test -p amigo-particles-2d-plugin
cargo test -p amigo-trails-2d-plugin

cargo test -p amigo-bloom-plugin
cargo test -p amigo-color-grading-plugin
cargo test -p amigo-scopes-plugin
cargo test -p amigo-composite-plugin
cargo test -p amigo-debug-views-plugin
```

---

# KROK 72 — finalne testy workspace

## OPERATION: VERIFY

```bash
cargo check --workspace
cargo test --workspace
cargo run -p plugin-check -- plugins
cargo run -p amigo-codemap -- plugins
```

---

# KROK 73 — finalny audyt struktury

## OPERATION: VERIFY

```bash
find plugins -maxdepth 2 -name plugin.toml | wc -l
find plugins -path '*render-wgpu*'
find crates -maxdepth 3 -type d | rg "2d/(sprite|text|vector|lighting|particles|post-fx|motion)|engine/camera"
rg "legacy|deprecated|_v2|re-export|reexport" crates plugins mods docs
rg "luma_fallback|should_produce_scene_highlight|direct_lens_flare|guess_optical" crates plugins mods
```

Expected:

```txt
- 27+ plugin.toml
- brak render-wgpu w src
- brak starych crate’ów domenowych
- brak legacy/deprecated/_v2
- brak renderer-side guessing
```

---

# KROK 74 — acceptance całej migracji

Projekt jest gotowy, jeśli:

```txt
1. Workspace nie ma missing members.
2. Każdy plugin ma canonical tree.
3. Każdy plugin ma Cargo.toml.
4. Każdy plugin ma plugin.toml.
5. Każdy plugin ma docs/pipeline.md.
6. Każdy plugin ma tests/waterfall_tests.rs.
7. Plugin-check przechodzi.
8. Codemap buduje plugin graph.
9. Scene nie trzyma domenowych komponentów.
10. Runtime bundles nie trzymają domenowych extractorów.
11. Render-wgpu nie zgaduje semantyki.
12. Scripting-rhai nie trzyma domenowych bindingów.
13. App/devtools tylko agregują plugin diagnostics.
14. Stare crate’y domenowe są usunięte.
15. Rotten Club używa contributions.
16. cargo check --workspace przechodzi.
17. cargo test --workspace przechodzi.
```

# Najkrótsza kolejność wykonania teraz

```txt
1. KROK 41 — workspace blockers.
2. KROK 44 — render_wgpu naming.
3. KROK 45 — canonical tree dla wszystkich pluginów.
4. KROK 46 — devtools/codemap Cargo.toml.
5. KROK 42 — plugin-check.
6. KROK 48 — migration inventory.
7. KROK 49–54 — camera family.
8. KROK 55–60 — gfx/lighting/materials/vfx/postfx.
9. KROK 61–65 — scene/runtime/render/scripting/app cleanup.
10. KROK 68–74 — content migration + final validation.
```

Najbliższy realny krok implementacyjny powinien być **KROK 41 + 44**, bo bez tego workspace i canonical tree są niespójne.
