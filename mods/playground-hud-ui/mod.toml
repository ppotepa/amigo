id = "playground-hud-ui"
name = "Playground HUD UI"
version = "0.1.0"
description = "Screen-space HUD/UI showcase for runtime themes and first interactive controls."
authors = ["Amigo Engine Team"]
dependencies = ["core"]
capabilities = ["screen_space_ui"]

[scripting]
mod_script_mode = "disabled"

[[scenes]]
id = "showcase"
label = "HUD/UI Showcase"
description = "Runtime themes, buttons, progress bars, slider, toggle and HUD/editor preview panels."
path = "scenes/showcase"
launcher_visible = true
