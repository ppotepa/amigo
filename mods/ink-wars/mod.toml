id = "ink-wars"
name = "INK WARS"
version = "0.1.0"
description = "Minimal INK WARS 2D menu scene for editor testing."
authors = ["Amigo Engine Team"]
dependencies = ["core"]
capabilities = ["rendering_2d", "vector_2d", "screen_space_ui"]
launcher_category = ["2D", "Editor Tests", "Playgrounds"]

[scripting]
mod_script_mode = "disabled"

[[scenes]]
id = "menu"
label = "INK WARS Menu"
description = "Simple transform-based 2D menu scene for editor canvas testing."
path = "scenes/menu"
launcher_visible = true

