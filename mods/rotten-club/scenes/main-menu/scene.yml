version: 1
scene:
  id: main-menu
  label: Rotten Club Main Menu
  description: Empty rebuild scene.

use:
  camera:
    - ./camera/main.yml
  render:
    - ./render/layers.yml
  world:
    - ./world/background.yml
    - ./world/depth.yml
