# Engine Task Docs

These files are archival implementation notes.

They may contain historical examples from the pre descriptor-first asset layout, including paths
such as `textures/...` or other deprecated examples. They are preserved as task history, not as the
current source of truth for asset layout.

Current asset layout and editor/runtime conventions live in:

- [docs/amigo-editor.asset-convention.md](../../amigo-editor.asset-convention.md)

When the two disagree, prefer the current convention document and the codebase.
