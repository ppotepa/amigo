# TASK 005 - Summary

## Status

- [x] Planned
- [x] In progress
- [x] Completed
- Note: completed as an early refactor slice and mechanical/domain-ownership checkpoint. The engine is not yet fully trait-first/plugin-first in the end-state described by the task, but the planned compatibility-preserving slice is done: `amigo-app` no longer owns the core scene/script dispatch mechanics alone, current 2D/3D scene command branches are split and mostly reduced to thin adapters, multiple domain-specific scene-command mappings moved into their owning crates, and app-local asset metadata knowledge was reduced for both tilemap and sprite paths. The remaining larger work belongs to follow-up tasks such as `platformer -> motion`, processor registries, hydrators, bundles, and deeper runtime decoupling.

## Validation

- [x] Confirm `crates/apps/app/src/lib.rs` is still a god-file
- [x] Confirm `SceneCommand` still knows many feature domains
- [x] Confirm `platformer_2d` still exists as an engine capability
- [x] Confirm Rhai still exposes `world.platformer`
- [x] Confirm diagnostics still use manual capability knowledge

## Early Slice Already Done

- [x] Add `RuntimeContext`
- [x] Add `required<T>()` / `optional<T>()` runtime wrapper
- [x] Extract `bootstrap` flow into `crates/apps/app/src/bootstrap.rs`
- [x] Extract app summary flow into `crates/apps/app/src/summary.rs`
- [x] Extract asset loading and hot reload watch sync into `crates/apps/app/src/assets/mod.rs`
- [x] Switch `stabilize_runtime(...)` to use extracted asset module
- [x] Switch `process_placeholder_bridges(...)` to use `RuntimeContext`
- [x] Extract runtime tick modules into `crates/apps/app/src/systems/*`
- [x] Switch host loop to call extracted `systems::*` modules
- [x] Extract `stabilize_runtime(...)` and placeholder bridge orchestration into `crates/apps/app/src/orchestration/mod.rs`
- [x] Extract `apply_scene_command(...)` path into `crates/apps/app/src/scene_runtime/command_apply.rs`
- [x] Extract `launch_selection` logic into `crates/apps/app/src/launch_selection.rs`
- [x] Extract scene loading/hydration helpers into `crates/apps/app/src/scene_runtime/*`
- [x] Extract script command dispatch into `crates/apps/app/src/script_runtime/mod.rs`
- [x] Extract script source/lifecycle helpers into `crates/apps/app/src/scripting_runtime.rs`
- [x] Extract host runtime glue into `crates/apps/app/src/host_runtime.rs`
- [x] Extract diagnostics plugin into `crates/apps/app/src/diagnostics.rs`
- [x] Extract UI runtime glue into `crates/apps/app/src/ui_runtime.rs`
- [x] Extract console/audio/asset bridge helpers into `crates/apps/app/src/orchestration/*`
- [x] Extract shared formatting / script path / asset reference helpers into `crates/apps/app/src/app_helpers.rs`
- [x] Add runtime-owned `SceneCommandHandlerRegistry` in `amigo-app` as compatibility stage
- [x] Split `Text2d`, `Mesh3d`, `Material3d`, and `Text3d` out of grouped scene command handlers into dedicated per-domain handlers
- [x] Split grouped `Physics2d` handling into `Body2d`, `Collider2d`, `Trigger2d`, and `Platformer2d` handlers
- [x] Split grouped `Sprite2d` and `TileMap2d` handling into dedicated per-domain handlers
- [x] Move UI scene-command support out of `ui_runtime` and into `scene_runtime`
- [x] Move the generic handler registry container out of `amigo-app` and into `amigo-runtime`
- [x] Back production `ScriptCommand` dispatch with a runtime-backed registry plugin
- [x] Move generic handler dispatch utility into `amigo-runtime`
- [x] Rename app-local scene/script execution contexts to make ownership explicit
- [x] Move generic routed handler trait/registry contract into `amigo-runtime`
- [x] Move sprite-sheet metadata inference and command sheet resolution into `amigo-2d-sprite`
- [x] Replace the huge `apply_scene_command(...)` call-site parameter list with runtime/context resolution inside `scene_runtime`
- [x] Replace lifecycle scene clearing call-sites with runtime-backed clearing helper
- [x] Replace the huge `dispatch_script_command_with_runtime(...)` call-site parameter list with runtime/context resolution inside `script_runtime`
- [x] Move shared `find_or_spawn_scene_entity` behavior from `amigo-app` helper code into `amigo-scene::SceneService`
- [x] Move `format_scene_command(...)` out of `amigo-app` helper code into `amigo-scene`
- [x] Keep behavior unchanged after the first slice

## Phase 1 - Mechanical App Split

- [x] Split `bootstrap` logic into its own module
- [x] Split `summary` logic into its own module
- [x] Split `launch_selection` logic into its own module
- [x] Split `assets/*`
- [x] Split `systems/*`
- [x] Split `orchestration/*`
- [x] Split `scene_runtime/command_apply.rs`
- [x] Split remaining `scene_runtime/*`
- [x] Split scripting lifecycle/source-prep helpers into `scripting_runtime.rs`
- [x] Split shared app helper functions into `app_helpers.rs`
- [ ] Reduce `app/src/lib.rs` to a thin entrypoint

## Phase 2 - Generic Runtime Context

- [x] Introduce `RuntimeContext`
- [x] Add dedicated aliases like `SceneCommandContext`, `SystemContext`, `AssetContext`
- [x] Replace large service parameter lists incrementally in `scene_runtime::apply_scene_command(...)`
- [x] Replace large service parameter lists incrementally in `script_runtime::dispatch_script_command_with_runtime(...)`
- [ ] Continue replacing large service parameter lists in remaining runtime glue

## Phase 3 - Plugin Bundles

- [ ] Add `PluginBundle` trait
- [ ] Add `RuntimeBuilder::with_bundle(...)`
- [ ] Introduce core bundles (`CoreEngineBundle`, `TwoDBundle`, `AudioBundle`, etc.)
- [ ] Replace long plugin chain in bootstrap with bundles

## Phase 4 - Neutralize Platformer into Motion2D

- [ ] Rename crate `crates/2d/platformer` -> `crates/2d/motion`
- [ ] Rename `PlatformerPlugin` -> `Motion2dPlugin`
- [ ] Rename `PlatformerSceneService` -> `Motion2dSceneService`
- [ ] Rename capability `platformer_2d` -> `motion_2d`
- [ ] Replace `world.platformer` with `world.motion`
- [ ] Keep sidescroller behavior working after rename

## Phase 5 - Sidescroller as Boilerplate

- [ ] Move sidescroller-specific Rhai helpers into boilerplate or mod-local libraries
- [ ] Remove engine-specific gameplay meaning from core domain names
- [ ] Keep `playground-sidescroller` working as sample content

## Phase 6 - SceneCommand Handler Registry

- [ ] Add `SceneCommandHandlerRegistry`
- [x] Add compatibility `SceneCommandDispatcher`
- [x] Back `SceneCommandDispatcher` with runtime-owned `SceneCommandHandlerRegistry`
- [x] Back scene command registration with a generic registry container from `amigo-runtime`
- [~] Move sprite scene command branch into plugin-owned handler
- [~] Move tilemap scene command branch into plugin-owned handler
- [~] Move physics scene command branch into plugin-owned handler
- [~] Move UI scene command branch into plugin-owned handler
- [~] Move text scene command branches into plugin-owned handlers
- [~] Move 3D render scene command branches into plugin-owned handlers
- [x] Move `Sprite2d` scene command branch into dedicated per-domain handler
- [x] Move `TileMap2d` scene command branch into dedicated per-domain handler
- [x] Move `Text2d` scene command branch into dedicated per-domain handler
- [x] Move `Mesh3d` scene command branch into dedicated per-domain handler
- [x] Move `Material3d` scene command branch into dedicated per-domain handler
- [x] Move `Text3d` scene command branch into dedicated per-domain handler
- [x] Split grouped `Physics2d` handler into dedicated per-domain handlers
- [x] Remove central domain branching from `apply_scene_command(...)`

## Phase 7 - Typed Command Bus

- [ ] Introduce typed command traits
- [ ] Keep `SceneCommand` only as compatibility layer if needed
- [ ] Move new work to typed commands first

## Phase 8 - Runtime Systems and Schedule

- [ ] Add `RuntimeSystem`
- [ ] Add `SystemRegistry`
- [ ] Add `SystemPhase`
- [x] Move motion tick to extracted system module (pre-schedule)
- [ ] Move physics tick to registered system
- [ ] Move trigger overlap tick to registered system
- [x] Move camera follow tick to extracted system module (pre-schedule)
- [x] Move parallax tick to extracted system module (pre-schedule)
- [x] Move audio tick to extracted system module (pre-schedule)
- [ ] Move UI input tick to registered system

## Phase 9 - Asset Processor Registry

- [ ] Add asset loader/preparer/processor registry
- [ ] Move sprite sheet metadata sync out of `app`
- [ ] Move tile ruleset sync out of `app`
- [ ] Move generated audio processing out of `app`

## Phase 10 - Rhai Imports and Libraries

- [ ] Add `ScriptImportResolver`
- [ ] Support engine libraries
- [ ] Support mod-local libraries
- [ ] Support scene-local libraries
- [ ] Make imports hot-reload aware

## Phase 11 - Plugin-Owned Rhai API

- [ ] Add script API provider registry
- [ ] Split monolithic `register_world_api(...)`
- [ ] Remove `RhaiScriptRuntime::new_with_platformer(...)`
- [ ] Let feature plugins register their own Rhai bindings

## Phase 12 - ScriptCommand Handler Registry

- [ ] Add `ScriptCommandHandlerRegistry`
- [x] Add compatibility `ScriptCommandDispatcher`
- [x] Back production `ScriptCommandDispatcher` with a runtime-backed registry plugin
- [ ] Move `scene.*` command handling out of `app`
- [ ] Move `audio.*` command handling out of `app`
- [ ] Move `ui.*` command handling out of `app`
- [x] Remove central `handle_script_command(...)`

## Phase 13 - Plugin-Owned Scene Hydrators

- [ ] Add `SceneComponentHydratorRegistry`
- [ ] Move sprite hydrator into plugin-owned code
- [ ] Move tilemap hydrator into plugin-owned code
- [ ] Move physics hydrator into plugin-owned code
- [ ] Move UI hydrator into plugin-owned code

## Phase 14 - Generic Component Storage

- [ ] Add generic `ComponentStore<T>`
- [ ] Add generic `ComponentService<T>`
- [ ] Move scene services away from repeated `Mutex<Vec<...>>`
- [ ] Keep `SceneEntityId` as primary runtime key
- [ ] Keep `entity_name` only as authored alias

## Phase 15 - Render Extractor Registry

- [ ] Add `RenderExtractorRegistry`
- [ ] Add `RenderFramePacket`
- [ ] Move sprite extraction out of `render-wgpu`
- [ ] Move tilemap extraction out of `render-wgpu`
- [ ] Move text/UI extraction out of `render-wgpu`

## Phase 16 - Capability Registry

- [ ] Add capability registry/provider abstraction
- [ ] Remove manual capability lists from diagnostics
- [ ] Remove explicit `platformer_2d` references from diagnostics

## Acceptance

- [x] Complete the planned compatibility-preserving refactor slice without regressions
- [x] Move shared scene/script dispatch mechanics below `amigo-app`
- [x] Split current scene command branches into dedicated handlers
- [x] Move first domain-owned scene-command logic into owning crates
- [x] Reduce app-owned asset metadata knowledge for tilemap and sprite
- [x] Keep tests green through the refactor slice

## Follow-up

- [x] Open follow-up task for `platformer -> motion`
- [x] Treat bundles / processors / hydrators / capability registry as later phases, not blockers for closing this slice
