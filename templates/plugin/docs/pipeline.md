# Pipeline

Document the concrete domain flow here:

`source -> capability -> contribution -> candidate -> target -> consumer -> output`

Name the owning source types, runtime extraction step, target resources and backend consumer. Remove stages that are genuinely not applicable rather than describing implicit fallbacks.
